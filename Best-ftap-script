– ██████████████████████████████████████████████████████████████████
– █                                                                  █
– █   SILENCED HUB  -  FOR ROBLOX SCRIPTS & DEV                     █
– █   Version: 1.0 beta  |  discord.gg/bdy4ssD3P4                        █
– █   Theme: Cyan/Black  |  Key Required                            █
– █                                                                  █
– ██████████████████████████████████████████████████████████████████

– ============================================================
–  SERVICES & LOCALS (needed before key screen)
– ============================================================
local Players            = game:GetService(“Players”)
local LocalPlayer        = Players.LocalPlayer
local CoreGui            = game:GetService(“CoreGui”)
local TweenService       = game:GetService(“TweenService”)
local UserInputService   = game:GetService(“UserInputService”)
local RunService         = game:GetService(“RunService”)
local ReplicatedStorage  = game:GetService(“ReplicatedStorage”)
local StarterGui         = game:GetService(“StarterGui”)

– ============================================================
–  SILENCED HUB  -  KEY SYSTEM
–  Keys are given in the Discord server: discord.gg/bdy4ssD3P4
– ============================================================

local VALID_KEYS = {
[“SILENCED-RO4S-BLAT-UMRS”] = true,
[“SILENCED-GOSH-1LKR-B20J”] = true,
[“SILENCED-B9ZS-T5IF-G80X”] = true,
[“SILENCED-QEX8-KG8T-BMEY”] = true,
[“SILENCED-M2W3-D2OG-MVVX”] = true,
[“SILENCED-D45Z-R5CV-O8GV”] = true,
[“SILENCED-7SKF-W8JZ-0EX9”] = true,
[“SILENCED-VY9Z-BUCN-TJ3M”] = true,
[“SILENCED-BQE9-J5FH-3258”] = true,
[“SILENCED-R04K-IX0E-DCRX”] = true,
[“SILENCED-WO6L-QWJC-E3BX”] = true,
[“SILENCED-RWJF-AANU-4SHJ”] = true,
[“SILENCED-IOVH-8UQ8-LXU2”] = true,
[“SILENCED-ZPJL-BTA4-HLWD”] = true,
[“SILENCED-KP9U-YEM0-QPGI”] = true,
[“SILENCED-7H91-II8I-VEE7”] = true,
[“SILENCED-43WX-IS9B-W0ZN”] = true,
[“SILENCED-4Z3Y-5XRE-S3FX”] = true,
[“SILENCED-QLH8-T1CV-FUKA”] = true,
[“SILENCED-I37M-0O8M-WPJ5”] = true,
[“SILENCED-RUZ0-TOZ3-KH47”] = true,
[“SILENCED-BTHY-AV9W-IP07”] = true,
[“SILENCED-FWWG-6DMA-ROKM”] = true,
[“SILENCED-E01K-1W5Q-C6N5”] = true,
[“SILENCED-WRPF-TBOE-ZMV9”] = true,
[“SILENCED-228J-LSZH-KMBP”] = true,
[“SILENCED-NRYA-ZF5T-OY4N”] = true,
[“SILENCED-D7C8-IPB3-769N”] = true,
[“SILENCED-HA8Q-EVCY-QYAL”] = true,
[“SILENCED-A82H-TLJ0-2BC0”] = true,
[“SILENCED-XUOK-CT7O-35T8”] = true,
[“SILENCED-1B70-CC5C-0CWE”] = true,
[“SILENCED-X7EL-WK08-8586”] = true,
[“SILENCED-7GPR-R3T3-V91C”] = true,
[“SILENCED-YD6B-PP6I-WGD3”] = true,
[“SILENCED-PRCW-VKON-RFHC”] = true,
[“SILENCED-PUYM-P277-08KK”] = true,
[“SILENCED-RE76-QAO3-7VFT”] = true,
[“SILENCED-9HV3-VPSE-4I2X”] = true,
[“SILENCED-HM2E-D33B-HVE2”] = true,
[“SILENCED-GB30-LRQ1-KNHE”] = true,
[“SILENCED-AE6B-B7JA-QD2N”] = true,
[“SILENCED-BX2K-E4AM-W31K”] = true,
[“SILENCED-6D3L-19ZN-VRUU”] = true,
[“SILENCED-CD1U-91KJ-TCGD”] = true,
[“SILENCED-MG0F-QNS3-BSZG”] = true,
[“SILENCED-04RI-IKS9-FSKI”] = true,
[“SILENCED-RE4G-IUCJ-TF7N”] = true,
[“SILENCED-HRIQ-ALVQ-KDI7”] = true,
[“SILENCED-DTXV-412K-KWOX”] = true,
[“SILENCED-8DMU-F4GF-5EB9”] = true,
[“SILENCED-EBFD-CX9F-XH5J”] = true,
[“SILENCED-X4LI-LDUN-3F8N”] = true,
[“SILENCED-A480-XETW-2GJF”] = true,
[“SILENCED-5O2R-1A84-DGSI”] = true,
[“SILENCED-28F1-OSWO-SUPA”] = true,
[“SILENCED-MNCI-F37K-5MSC”] = true,
[“SILENCED-Z97V-J2KR-N9EJ”] = true,
[“SILENCED-KY9T-5XYP-4Q8X”] = true,
[“SILENCED-J0AZ-HTQU-9RYE”] = true,
[“SILENCED-ZY28-3OR7-DXG0”] = true,
[“SILENCED-720D-AF4X-F725”] = true,
[“SILENCED-9NYK-A6RE-XH9A”] = true,
[“SILENCED-YM0T-P56W-T4OJ”] = true,
[“SILENCED-LJ9L-3ZVY-R4KM”] = true,
[“SILENCED-9J0V-HH78-1BTH”] = true,
[“SILENCED-M08R-JBAV-7F6X”] = true,
[“SILENCED-CCBA-CMEX-P87Q”] = true,
[“SILENCED-CYW2-3LBQ-0G4D”] = true,
[“SILENCED-LT7X-P5UM-CYFP”] = true,
[“SILENCED-8AM0-B27O-ZNUQ”] = true,
[“SILENCED-LZR5-MYBF-RRC3”] = true,
[“SILENCED-XVIB-U2R0-696L”] = true,
[“SILENCED-Q7W1-VQG8-2IWI”] = true,
[“SILENCED-ES39-96VX-HJKF”] = true,
[“SILENCED-IRZL-Y2OO-89AB”] = true,
[“SILENCED-SXON-E5Z7-NJBF”] = true,
[“SILENCED-CL0G-MBBM-VFNO”] = true,
[“SILENCED-T4UU-7OYV-EHY3”] = true,
[“SILENCED-S3PS-XCYM-VHL0”] = true,
[“SILENCED-SO8H-3DVB-1Q2U”] = true,
[“SILENCED-91NB-GVOP-505T”] = true,
[“SILENCED-A95M-L0PR-SE4S”] = true,
[“SILENCED-W5AG-QDXK-WNRQ”] = true,
[“SILENCED-AVEU-OJE1-0FB6”] = true,
[“SILENCED-AKUL-G0Q6-MR4G”] = true,
[“SILENCED-97F6-V1EB-6YDX”] = true,
[“SILENCED-32FW-P0YP-EFAZ”] = true,
[“SILENCED-QPLC-J5QX-EQQT”] = true,
[“SILENCED-9EIU-X0R5-62FT”] = true,
[“SILENCED-ZM02-3GX0-3632”] = true,
[“SILENCED-LNJM-394G-6DEL”] = true,
[“SILENCED-MM5B-IHBX-X76Y”] = true,
[“SILENCED-3VKU-JSR6-VQZO”] = true,
[“SILENCED-6V3S-SW2M-O6H5”] = true,
[“SILENCED-3UD5-NN0Z-M50L”] = true,
[“SILENCED-MJTL-04ZP-P3I8”] = true,
[“SILENCED-B1RX-JB52-CVKK”] = true,
[“SILENCED-36XK-3OOA-XTKF”] = true,
[“SILENCED-TD3K-SW6M-TMIX”] = true,
[“SILENCED-A861-JPVH-YHMS”] = true,
[“SILENCED-6QGE-J68F-IDMB”] = true,
[“SILENCED-K4ED-YK9N-A2RP”] = true,
[“SILENCED-OBPH-69L9-C7KW”] = true,
[“SILENCED-4IZA-ZJPM-M152”] = true,
[“SILENCED-HAGZ-0G0L-9YRY”] = true,
[“SILENCED-NG82-G0JL-9OHL”] = true,
[“SILENCED-TBH4-UITG-S4UU”] = true,
[“SILENCED-G8A4-NLLB-0FJT”] = true,
[“SILENCED-4T20-CJOK-5IUG”] = true,
[“SILENCED-ZLN3-RGP2-PNWT”] = true,
[“SILENCED-AG0M-QUS6-TM10”] = true,
[“SILENCED-T2AG-BY78-PEKZ”] = true,
[“SILENCED-A8I8-UBPP-ICH4”] = true,
[“SILENCED-IN68-3D5R-NVVM”] = true,
[“SILENCED-DSDY-YFCK-0E8E”] = true,
[“SILENCED-CVW3-AHDW-D23J”] = true,
[“SILENCED-1W0E-F9K2-QIHU”] = true,
[“SILENCED-ONHO-RQP9-3XN3”] = true,
[“SILENCED-X7NU-P5PU-4JNG”] = true,
[“SILENCED-X6UN-VN1T-437X”] = true,
[“SILENCED-O09U-05JU-DJB5”] = true,
[“SILENCED-N54P-YXGF-413Z”] = true,
[“SILENCED-WVNY-M1YB-FYT5”] = true,
[“SILENCED-ZYN2-LK4S-K8IS”] = true,
[“SILENCED-P8Y9-ITE8-PAB1”] = true,
[“SILENCED-5FHD-RGR1-4W5P”] = true,
[“SILENCED-9TL2-TMVP-BVM2”] = true,
[“SILENCED-3AWO-UNSA-GCUI”] = true,
[“SILENCED-K09N-9T8A-CFA6”] = true,
[“SILENCED-ES5K-Y57Y-TYUS”] = true,
[“SILENCED-ZEF1-DTW5-G29A”] = true,
[“SILENCED-99O9-BC03-ZMVN”] = true,
[“SILENCED-T36R-4F05-YCWG”] = true,
[“SILENCED-HJGE-L6RB-QU52”] = true,
[“SILENCED-WO3H-0XXG-WHUY”] = true,
[“SILENCED-DCI2-2PX0-Q481”] = true,
[“SILENCED-WEP7-X2TA-XQQC”] = true,
[“SILENCED-WAAK-TCR1-OGJF”] = true,
[“SILENCED-T1MT-EFFD-DVKV”] = true,
[“SILENCED-0YSL-IZ9N-YV2I”] = true,
[“SILENCED-AHNZ-WMS0-YOAA”] = true,
[“SILENCED-Z48L-0AXU-VFMV”] = true,
[“SILENCED-SVWO-QP0R-BUUN”] = true,
[“SILENCED-OYY7-U7RX-MH00”] = true,
[“SILENCED-UZ7I-C61M-W484”] = true,
[“SILENCED-JBNL-D1NA-1Z8D”] = true,
[“SILENCED-Q5S7-EUF0-V1E5”] = true,
[“SILENCED-JTXE-XIWP-B1EV”] = true,
[“SILENCED-D0IU-AZKK-UM7H”] = true,
[“SILENCED-70A5-PDSZ-DPPY”] = true,
[“SILENCED-CW6L-T0SL-NR5M”] = true,
[“SILENCED-Z4J2-GLI9-5AN5”] = true,
[“SILENCED-HYHR-D8AC-OZVN”] = true,
[“SILENCED-Z46D-ZRVY-55ZH”] = true,
[“SILENCED-U585-SID4-NBG0”] = true,
[“SILENCED-3LNY-P99U-1C9U”] = true,
[“SILENCED-AFX2-QYVZ-B7OB”] = true,
[“SILENCED-EQHA-TG32-5IPR”] = true,
[“SILENCED-087F-H8A8-YSSY”] = true,
[“SILENCED-FDKA-XQ1N-4SJN”] = true,
[“SILENCED-CJ82-MGFR-OFWM”] = true,
[“SILENCED-XW5G-TQXJ-ZMFX”] = true,
[“SILENCED-P5ZP-GXVL-63TN”] = true,
[“SILENCED-Z2P2-1OAB-7UJZ”] = true,
[“SILENCED-TBH1-VR1I-6Y26”] = true,
[“SILENCED-OXVZ-2S3R-X9BV”] = true,
[“SILENCED-195W-5KLF-KZ89”] = true,
[“SILENCED-HB1Q-6IPI-2XYV”] = true,
[“SILENCED-TZWS-D89C-S5NN”] = true,
[“SILENCED-225G-AQ5K-0SS9”] = true,
[“SILENCED-JMAW-BINE-EFZO”] = true,
[“SILENCED-QPTA-QFO6-GVA5”] = true,
[“SILENCED-K3NG-CDSI-L2AO”] = true,
[“SILENCED-YLGU-ONBA-OX5S”] = true,
[“SILENCED-NLK0-CORT-AN87”] = true,
[“SILENCED-UEN6-LTDG-6DQF”] = true,
[“SILENCED-N0Q3-F2K9-WZA7”] = true,
[“SILENCED-IP08-BNSK-64YY”] = true,
[“SILENCED-LY7C-SP6T-PB7S”] = true,
[“SILENCED-7LDT-A3S7-RRKH”] = true,
[“SILENCED-CM5C-BZ0B-DRM6”] = true,
[“SILENCED-D2QN-I02D-RHPF”] = true,
[“SILENCED-U2JA-FB0G-6500”] = true,
[“SILENCED-8LPI-90Z8-45EC”] = true,
[“SILENCED-0PG1-JN8B-ILVA”] = true,
[“SILENCED-C1JT-TLTB-HYLK”] = true,
[“SILENCED-CGZP-U5SP-271S”] = true,
[“SILENCED-UET2-FP78-7J0T”] = true,
[“SILENCED-HTVY-6ODS-ULUU”] = true,
[“SILENCED-SCKA-A6UA-ECJS”] = true,
[“SILENCED-IH5T-5UFF-47HH”] = true,
[“SILENCED-XA0Y-4YXO-X7PA”] = true,
[“SILENCED-BV5B-SXT8-1813”] = true,
[“SILENCED-KFQZ-C570-WHGQ”] = true,
[“SILENCED-7I7N-VYEN-ZH7I”] = true,
[“SILENCED-5XTV-WOYG-YDZD”] = true,
[“SILENCED-8T48-AGYU-DRN1”] = true,
[“SILENCED-IMR8-0VP8-IBV4”] = true,
[“SILENCED-9BEB-5A8O-YF9X”] = true,
}

local DISCORD_LINK = “discord.gg/bdy4ssD3P4”
local KEY_SAVE_FILE = “SilencedHub_Key.txt”

– ============================================================
–  KEY SCREEN UI
– ============================================================

local keyVerified = false

local function buildKeyScreen()
– Remove any existing key screen
if CoreGui:FindFirstChild(“SilencedKeySystem”) then
CoreGui.SilencedKeySystem:Destroy()
end

```
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "SilencedKeySystem"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = CoreGui

-- Dark overlay
local Overlay = Instance.new("Frame")
Overlay.Size = UDim2.new(1, 0, 1, 0)
Overlay.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Overlay.BackgroundTransparency = 0.35
Overlay.BorderSizePixel = 0
Overlay.Parent = ScreenGui

-- Main card
local Card = Instance.new("Frame")
Card.Size = UDim2.new(0, 500, 0, 420)
Card.Position = UDim2.new(0.5, -250, 0.5, -210)
Card.BackgroundColor3 = Color3.fromRGB(8, 8, 12)
Card.BorderSizePixel = 0
Card.Parent = ScreenGui

local CardCorner = Instance.new("UICorner")
CardCorner.CornerRadius = UDim.new(0, 14)
CardCorner.Parent = Card

-- Cyan glow border stroke
local CardStroke = Instance.new("UIStroke")
CardStroke.Color = Color3.fromRGB(0, 210, 255)
CardStroke.Thickness = 2
CardStroke.Transparency = 0.15
CardStroke.Parent = Card

-- Top accent bar
local TopBar = Instance.new("Frame")
TopBar.Size = UDim2.new(1, 0, 0, 4)
TopBar.BackgroundColor3 = Color3.fromRGB(0, 210, 255)
TopBar.BorderSizePixel = 0
TopBar.Parent = Card
local TopBarCorner = Instance.new("UICorner")
TopBarCorner.CornerRadius = UDim.new(0, 14)
TopBarCorner.Parent = TopBar

-- Logo area (top) - with image
local LogoImage = Instance.new("ImageLabel")
LogoImage.Size = UDim2.new(0, 120, 0, 120)
LogoImage.Position = UDim2.new(0.5, -60, 0, 5)
LogoImage.BackgroundTransparency = 1
LogoImage.Image = "rbxassetid://75799177950528" -- Roblox hosted, fallback to text if not available
LogoImage.ScaleType = Enum.ScaleType.Fit
LogoImage.Parent = Card
pcall(function() LogoImage.Image = "rbxassetid://75799177950528" end)

local LogoLabel = Instance.new("TextLabel")
LogoLabel.Size = UDim2.new(1, 0, 0, 40)
LogoLabel.Position = UDim2.new(0, 0, 0, 120)
LogoLabel.BackgroundTransparency = 1
LogoLabel.Text = "SILENCED HUB"
LogoLabel.Font = Enum.Font.GothamBold
LogoLabel.TextSize = 28
LogoLabel.TextColor3 = Color3.fromRGB(0, 210, 255)
LogoLabel.Parent = Card

-- Glow effect on logo
local LogoShadow = Instance.new("TextLabel")
LogoShadow.Size = UDim2.new(1, 0, 0, 40)
LogoShadow.Position = UDim2.new(0, 2, 0, 122)
LogoShadow.BackgroundTransparency = 1
LogoShadow.Text = "SILENCED HUB"
LogoShadow.Font = Enum.Font.GothamBold
LogoShadow.TextSize = 28
LogoShadow.TextColor3 = Color3.fromRGB(0, 100, 140)
LogoShadow.ZIndex = 0
LogoShadow.Parent = Card

local SubLabel = Instance.new("TextLabel")
SubLabel.Size = UDim2.new(1, 0, 0, 22)
SubLabel.Position = UDim2.new(0, 0, 0, 88)
SubLabel.BackgroundTransparency = 1
SubLabel.Text = "FOR ROBLOX SCRIPTS & DEV"
SubLabel.Font = Enum.Font.GothamSemibold
SubLabel.TextSize = 13
SubLabel.TextColor3 = Color3.fromRGB(100, 200, 230)
SubLabel.LetterSpacingEm = 0.15
SubLabel.Parent = Card

-- Divider
local Divider = Instance.new("Frame")
Divider.Size = UDim2.new(0.85, 0, 0, 1)
Divider.Position = UDim2.new(0.075, 0, 0, 120)
Divider.BackgroundColor3 = Color3.fromRGB(0, 210, 255)
Divider.BackgroundTransparency = 0.6
Divider.BorderSizePixel = 0
Divider.Parent = Card

-- Key prompt text
local PromptLabel = Instance.new("TextLabel")
PromptLabel.Size = UDim2.new(0.9, 0, 0, 30)
PromptLabel.Position = UDim2.new(0.05, 0, 0, 136)
PromptLabel.BackgroundTransparency = 1
PromptLabel.Text = "Enter your key below to continue"
PromptLabel.Font = Enum.Font.Gotham
PromptLabel.TextSize = 14
PromptLabel.TextColor3 = Color3.fromRGB(160, 190, 210)
PromptLabel.Parent = Card

-- TextBox background
local InputBg = Instance.new("Frame")
InputBg.Size = UDim2.new(0.88, 0, 0, 46)
InputBg.Position = UDim2.new(0.06, 0, 0, 172)
InputBg.BackgroundColor3 = Color3.fromRGB(14, 18, 28)
InputBg.BorderSizePixel = 0
InputBg.Parent = Card
local InputCorner = Instance.new("UICorner")
InputCorner.CornerRadius = UDim.new(0, 8)
InputCorner.Parent = InputBg
local InputStroke = Instance.new("UIStroke")
InputStroke.Color = Color3.fromRGB(0, 160, 200)
InputStroke.Thickness = 1.5
InputStroke.Transparency = 0.4
InputStroke.Parent = InputBg

-- TextBox
local KeyBox = Instance.new("TextBox")
KeyBox.Size = UDim2.new(1, -16, 1, 0)
KeyBox.Position = UDim2.new(0, 8, 0, 0)
KeyBox.BackgroundTransparency = 1
KeyBox.Font = Enum.Font.GothamMono
KeyBox.TextSize = 14
KeyBox.TextColor3 = Color3.fromRGB(0, 230, 255)
KeyBox.PlaceholderText = "SILENCED-XXXX-XXXX"
KeyBox.PlaceholderColor3 = Color3.fromRGB(60, 100, 120)
KeyBox.ClearTextOnFocus = false
KeyBox.Text = ""
KeyBox.Parent = InputBg

-- Status label
local StatusLabel = Instance.new("TextLabel")
StatusLabel.Size = UDim2.new(0.88, 0, 0, 22)
StatusLabel.Position = UDim2.new(0.06, 0, 0, 222)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = ""
StatusLabel.Font = Enum.Font.GothamSemibold
StatusLabel.TextSize = 12
StatusLabel.TextColor3 = Color3.fromRGB(255, 80, 80)
StatusLabel.Parent = Card

-- Submit Button
local SubmitBtn = Instance.new("TextButton")
SubmitBtn.Size = UDim2.new(0.88, 0, 0, 46)
SubmitBtn.Position = UDim2.new(0.06, 0, 0, 250)
SubmitBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 220)
SubmitBtn.Font = Enum.Font.GothamBold
SubmitBtn.TextSize = 15
SubmitBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
SubmitBtn.Text = "▶  VERIFY KEY"
SubmitBtn.BorderSizePixel = 0
SubmitBtn.AutoButtonColor = false
SubmitBtn.Parent = Card
local SubmitCorner = Instance.new("UICorner")
SubmitCorner.CornerRadius = UDim.new(0, 8)
SubmitCorner.Parent = SubmitBtn

-- Hover effect
SubmitBtn.MouseEnter:Connect(function()
    TweenService:Create(SubmitBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(0, 210, 255)}):Play()
end)
SubmitBtn.MouseLeave:Connect(function()
    TweenService:Create(SubmitBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(0, 170, 220)}):Play()
end)

-- Discord button
local DiscordBtn = Instance.new("TextButton")
DiscordBtn.Size = UDim2.new(0.88, 0, 0, 40)
DiscordBtn.Position = UDim2.new(0.06, 0, 0, 306)
DiscordBtn.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
DiscordBtn.Font = Enum.Font.GothamSemibold
DiscordBtn.TextSize = 13
DiscordBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
DiscordBtn.Text = "🔗  Join Discord to Get a Key  →  discord.gg/bdy4ssD3P4"
DiscordBtn.BorderSizePixel = 0
DiscordBtn.AutoButtonColor = false
DiscordBtn.Parent = Card
local DiscordCorner = Instance.new("UICorner")
DiscordCorner.CornerRadius = UDim.new(0, 8)
DiscordCorner.Parent = DiscordBtn

DiscordBtn.MouseEnter:Connect(function()
    TweenService:Create(DiscordBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(110, 120, 255)}):Play()
end)
DiscordBtn.MouseLeave:Connect(function()
    TweenService:Create(DiscordBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(88, 101, 242)}):Play()
end)

-- Footer
local FooterLabel = Instance.new("TextLabel")
FooterLabel.Size = UDim2.new(1, 0, 0, 22)
FooterLabel.Position = UDim2.new(0, 0, 0, 388)
FooterLabel.BackgroundTransparency = 1
FooterLabel.Text = "Keys are free — join the Discord to get yours!"
FooterLabel.Font = Enum.Font.Gotham
FooterLabel.TextSize = 11
FooterLabel.TextColor3 = Color3.fromRGB(80, 120, 140)
FooterLabel.Parent = Card

-- Animated cyan scan line
local ScanLine = Instance.new("Frame")
ScanLine.Size = UDim2.new(1, 0, 0, 2)
ScanLine.Position = UDim2.new(0, 0, 0, 0)
ScanLine.BackgroundColor3 = Color3.fromRGB(0, 210, 255)
ScanLine.BackgroundTransparency = 0.7
ScanLine.BorderSizePixel = 0
ScanLine.ZIndex = 2
ScanLine.Parent = Card

task.spawn(function()
    while Card and Card.Parent do
        TweenService:Create(ScanLine, TweenInfo.new(2, Enum.EasingStyle.Linear), {Position = UDim2.new(0, 0, 1, -2)}):Play()
        task.wait(2)
        ScanLine.Position = UDim2.new(0, 0, 0, 0)
        task.wait(0.05)
    end
end)

-- Intro animation
Card.Position = UDim2.new(0.5, -250, 0.5, -160)
Card.BackgroundTransparency = 1
TweenService:Create(Card, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
    Position = UDim2.new(0.5, -250, 0.5, -210),
    BackgroundTransparency = 0
}):Play()

-- ---- Verify logic ----
local function tryVerify(inputKey)
    local trimmed = inputKey:match("^%s*(.-)%s*$") -- trim whitespace
    if VALID_KEYS[trimmed] then
        -- Save key for next time
        pcall(function()
            if writefile then writefile(KEY_SAVE_FILE, trimmed) end
        end)
        StatusLabel.TextColor3 = Color3.fromRGB(0, 230, 130)
        StatusLabel.Text = "✔  Key accepted! Loading Silenced Hub..."
        TweenService:Create(SubmitBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(0, 200, 100)}):Play()
        task.wait(1.2)
        -- Fade out
        TweenService:Create(ScreenGui, TweenInfo.new(0.4), {AutoLocalize = false}):Play()
        TweenService:Create(Card, TweenInfo.new(0.4), {BackgroundTransparency = 1}):Play()
        TweenService:Create(Overlay, TweenInfo.new(0.4), {BackgroundTransparency = 1}):Play()
        task.wait(0.45)
        ScreenGui:Destroy()
        keyVerified = true
    else
        StatusLabel.TextColor3 = Color3.fromRGB(255, 80, 80)
        StatusLabel.Text = "✘  Invalid key! Join Discord to get yours."
        -- Shake animation
        for i = 1, 6 do
            TweenService:Create(Card, TweenInfo.new(0.05), {Position = UDim2.new(0.5, -250 + (i%2==0 and 8 or -8), 0.5, -210)}):Play()
            task.wait(0.05)
        end
        TweenService:Create(Card, TweenInfo.new(0.1), {Position = UDim2.new(0.5, -250, 0.5, -210)}):Play()
    end
end

SubmitBtn.MouseButton1Click:Connect(function()
    tryVerify(KeyBox.Text)
end)

KeyBox.FocusLost:Connect(function(enter)
    if enter then tryVerify(KeyBox.Text) end
end)

DiscordBtn.MouseButton1Click:Connect(function()
    -- Copy invite link to clipboard so user can paste it into browser/Discord
    pcall(function()
        if setclipboard then
            setclipboard("https://discord.gg/bdy4ssD3P4")
        end
    end)
    -- Also try to open via shell on supported executors (Synapse X, KRNL, etc.)
    pcall(function()
        if syn and syn.request then
            syn.request({Url = "https://discord.gg/bdy4ssD3P4", Method = "GET"})
        end
    end)
    pcall(function()
        if shell_execute then shell_execute("https://discord.gg/bdy4ssD3P4") end
    end)
    -- Visual feedback
    local oldText = DiscordBtn.Text
    DiscordBtn.Text = "✅  Link copied to clipboard!"
    TweenService:Create(DiscordBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(50, 200, 100)}):Play()
    StatusLabel.TextColor3 = Color3.fromRGB(0, 230, 130)
    StatusLabel.Text = "📋  Paste in browser: discord.gg/bdy4ssD3P4"
    task.wait(3)
    DiscordBtn.Text = oldText
    TweenService:Create(DiscordBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(88, 101, 242)}):Play()
    StatusLabel.Text = ""
end)

-- Focus the box
task.wait(0.6)
KeyBox:CaptureFocus()
```

end

– Key system bypassed
keyVerified = true

– ============================================================
–  KEY VERIFIED — LOAD THE HUB
– ============================================================

– Disable beam move script (safe)
pcall(function()
game.Players.LocalPlayer.PlayerScripts.CharacterAndBeamMove.Enabled = false
end)

– Load Infinite Yield quietly
loadstring(game:HttpGet(‘https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source’))()

– Load XOCU library
local Library = loadstring(game:HttpGet(“https://raw.githubusercontent.com/iavichbest/XOCU/refs/heads/main/UI”))()

– Unlock Mouse
UserInputService.MouseIconEnabled = true
UserInputService.MouseBehavior = Enum.MouseBehavior.Default

– Global state storage
local ToggleStates = {}
local function SetToggleState(flag, value) ToggleStates[flag] = value end
local function GetToggleState(flag) return ToggleStates[flag] or false end

local Options = Library.Items or Library.Flags or {}
local Toggles = Library.Flags or Library.Items or {}

– ============================================================
–  WINDOW — SILENCED HUB BRANDING
– ============================================================
local Window = Library:CreateWindow({
Title         = “SILENCED HUB”,
Theme         = “Void”,
ToggleKey     = Enum.KeyCode.RightShift,
Transparency  = 0.18,
ShowWatermark = { Enabled = true, Title = true, User = true, FPS = true, Duration = false, Ping = true },
AutoSave      = true,
ConfigFolder  = “SilencedHub_Config”
})

– ============================================================
–  TABS
– ============================================================
local Tabs = {
Defense  = Window:CreateTab(“defense”,  true),
Target   = Window:CreateTab(“target”,   true),
Grab     = Window:CreateTab(“grab”,     true),
Player   = Window:CreateTab(“player”,   true),
Misc     = Window:CreateTab(“misc”,     true),
Build    = Window:CreateTab(“build”,    true),
Fun      = Window:CreateTab(“fun”,      true),
Keybinds = Window:CreateTab(“keybinds”, true),
Auras    = Window:CreateTab(“auras”,    true),
}

– ============================================================
–  SERVICES RE-BINDS
– ============================================================
local PS        = game:GetService(“Players”)
local RS        = game:GetService(“ReplicatedStorage”)
local R         = game:GetService(“RunService”)
local Workspace = workspace
local Player    = PS.LocalPlayer
local Camera    = Workspace.CurrentCamera
local CE        = RS:WaitForChild(“CharacterEvents”, 10)
local BeingHeld = Player:WaitForChild(“IsHeld”, 10)
local StruggleEvent = CE and CE:WaitForChild(“Struggle”)

– ============================================================
–  NOTIFY HELPER
– ============================================================
local function notify(title, content, duration)
Library:Notify({
Title    = title   or “Silenced Hub”,
Content  = content or “”,
Duration = duration or 5,
})
end

– ============================================================
–  STARTUP MESSAGE
– ============================================================
local function sendHubLoadedMessage()
local message = “ ⚙ SILENCED HUB loaded — discord.gg/bdy4ssD3P4 “
local sent = false
pcall(function()
local chatEvents = ReplicatedStorage:FindFirstChild(“DefaultChatSystemChatEvents”)
if chatEvents then
local say = chatEvents:FindFirstChild(“SayMessageRequest”)
if say and typeof(say.FireServer) == “function” then
say:FireServer(message, “All”)
sent = true
end
end
end)
if not sent then
pcall(function()
StarterGui:SetCore(“ChatMakeSystemMessage”, {
Name     = message;
Color    = Color3.fromRGB(0, 210, 255);
Font     = Enum.Font.GothamBold;
FontSize = Enum.FontSize.Size18;
})
end)
end
end

task.spawn(function()
task.wait(1)
sendHubLoadedMessage()
notify(“SILENCED HUB”, “Welcome! Hub loaded successfully. Stay silenced. 🤫”, 5)
end)

– ============================================================
–  PAINT PARTS HELPERS
– ============================================================
local paintPartsBackup = {}
local paintConnections = {}

local function deleteAllPaintParts()
for _, obj in ipairs(Workspace:GetDescendants()) do
if obj:IsA(“BasePart”) and obj.Name == “PaintPlayerPart” then
local clone = obj:Clone()
clone.Archivable = true
paintPartsBackup[obj:GetDebugId()] = { clone = clone, parent = obj.Parent }
obj:Destroy()
end
end
end

local function restorePaintParts()
for _, data in pairs(paintPartsBackup) do
if data.clone and data.parent then
data.clone.Parent = data.parent
end
end
paintPartsBackup = {}
end

local function watchNewPaintParts()
table.insert(paintConnections, Workspace.DescendantAdded:Connect(function(obj)
if obj:IsA(“BasePart”) and obj.Name == “PaintPlayerPart” then
task.defer(function()
if obj and obj.Parent then
local clone = obj:Clone()
clone.Archivable = true
paintPartsBackup[obj:GetDebugId()] = { clone = clone, parent = obj.Parent }
obj:Destroy()
end
end)
end
end))
end

local function disconnectWatchers()
for _, conn in ipairs(paintConnections) do
if conn.Connected then conn:Disconnect() end
end
paintConnections = {}
end

local function setTouchQuery(state)
local char = Workspace:FindFirstChild(Player.Name)
if not char then return end
for _, v in ipairs(char:GetChildren()) do
if v:IsA(“Part”) or v:IsA(“BasePart”) then
v.CanTouch = state
v.CanQuery = state
end
end
end

– ============================================================
–  BLOBMAN / ANTI-GUCCI HELPERS
– ============================================================
local antiGucciConnection
local safePosition
local restoreFrames = 0

local function spawnBlobman()
local args = {
[1] = “CreatureBlobman”,
[2] = CFrame.new(0, 5000000, 0),
[3] = Vector3.new(0, 60, 0)
}
pcall(function()
ReplicatedStorage.MenuToys.SpawnToyRemoteFunction:InvokeServer(unpack(args))
end)
local folder = Workspace:WaitForChild(Player.Name .. “SpawnedInToys”, 5)
if folder and folder:FindFirstChild(“CreatureBlobman”) then
local blob = folder.CreatureBlobman
if blob:FindFirstChild(“Head”) then
blob.Head.CFrame = CFrame.new(0, 50000, 0)
blob.Head.Anchored = true
end
notify(“Silenced Hub”, “Blobman Spawned!”, 3)
end
end

local function startAntiGucci()
local character = Player.Character or Player.CharacterAdded:Wait()
local humanoid  = character:WaitForChild(“Humanoid”)
local rootPart  = character:WaitForChild(“HumanoidRootPart”)
safePosition = rootPart.Position
local folder = Workspace:FindFirstChild(Player.Name .. “SpawnedInToys”)
local blob   = folder and folder:FindFirstChild(“CreatureBlobman”)
local seat   = blob and blob:FindFirstChild(“VehicleSeat”)
if not blob then
spawnBlobman()
task.wait(1)
folder = Workspace:FindFirstChild(Player.Name .. “SpawnedInToys”)
blob   = folder and folder:FindFirstChild(“CreatureBlobman”)
seat   = blob and blob:FindFirstChild(“VehicleSeat”)
end
if seat and seat:IsA(“VehicleSeat”) then
rootPart.CFrame = seat.CFrame + Vector3.new(0, 2, 0)
seat:Sit(humanoid)
end
humanoid:GetPropertyChangedSignal(“Jump”):Connect(function()
if humanoid.Jump and humanoid.Sit then
restoreFrames = 15
safePosition = rootPart.Position
end
end)
if antiGucciConnection then antiGucciConnection:Disconnect() end
antiGucciConnection = R.Heartbeat:Connect(function()
if not rootPart or not humanoid then return end
ReplicatedStorage.CharacterEvents.RagdollRemote:FireServer(rootPart, 0)
if restoreFrames > 0 then
rootPart.CFrame = CFrame.new(safePosition)
restoreFrames = restoreFrames - 1
end
end)
task.spawn(function()
while humanoid.Sit do task.wait(1) end
task.wait(0.5)
rootPart.CFrame = CFrame.new(safePosition)
end)
end

local function stopAntiGucci()
if antiGucciConnection then
antiGucciConnection:Disconnect()
antiGucciConnection = nil
end
local blobFolder = Workspace:FindFirstChild(Player.Name .. “SpawnedInToys”)
if blobFolder and blobFolder:FindFirstChild(“CreatureBlobman”) then
blobFolder.CreatureBlobman:Destroy()
end
end

local antiGucciConnectionTrain
local safePositionTrain
local restoreFramesTrain = 0

local function startAntiGucciTrain()
local character = Player.Character or Player.CharacterAdded:Wait()
local humanoid  = character:WaitForChild(“Humanoid”)
local rootPart  = character:WaitForChild(“HumanoidRootPart”)
safePositionTrain = rootPart.Position
local folder = workspace.Map.AlwaysHereTweenedObjects
local train  = folder and folder:FindFirstChild(“Train”)
local seat
if train then
for _, d in ipairs(train:GetDescendants()) do
if d:IsA(“Seat”) then seat = d; break end
end
end
if seat then
rootPart.CFrame = seat.CFrame + Vector3.new(0, 2, 0)
seat:Sit(humanoid)
end
humanoid:GetPropertyChangedSignal(“Jump”):Connect(function()
if humanoid.Jump and humanoid.Sit then
restoreFramesTrain = 15
safePositionTrain = rootPart.Position
end
end)
if antiGucciConnectionTrain then antiGucciConnectionTrain:Disconnect() end
antiGucciConnectionTrain = R.Heartbeat:Connect(function()
if not rootPart or not humanoid then return end
ReplicatedStorage.CharacterEvents.RagdollRemote:FireServer(rootPart, 0)
if restoreFramesTrain > 0 then
rootPart.CFrame = CFrame.new(safePositionTrain)
restoreFramesTrain = restoreFramesTrain - 1
end
end)
task.spawn(function()
while humanoid.Sit do task.wait(1) end
task.wait(0.5)
rootPart.CFrame = CFrame.new(safePositionTrain)
end)
end

local function stopAntiGucciTrain()
if antiGucciConnectionTrain then
antiGucciConnectionTrain:Disconnect()
antiGucciConnectionTrain = nil
end
local trainFolder = workspace.Map.AlwaysHereTweenedObjects
if trainFolder and trainFolder:FindFirstChild(“Train”) then
– Reset player
end
end

– ============================================================
–  DEFENSE TAB
– ============================================================
local DefenseGroup = Tabs.Defense:CreateBlock({Name = “Defense Main”, Side = “Left”})
local DefenseExtra = Tabs.Defense:CreateBlock({Name = “Extra Defense”, Side = “Right”})

local antiGrabExplosionConn, antiGrabHeldConn, antiGrabStruggleConn, antiGrabHumConn, antiGrabAnchorConn
local antiGrabRootCF, antiGrabRootPos, antiGrabHardFreeze = nil, nil, false

local function antiGrabUnfreeze(char)
local hrp = char and char:FindFirstChild(“HumanoidRootPart”)
if hrp then
hrp.Anchored = false
if hrp:FindFirstChild(“FreezeJoint”) then hrp.FreezeJoint:Destroy() end
end
antiGrabHardFreeze = false
if antiGrabAnchorConn then antiGrabAnchorConn:Disconnect(); antiGrabAnchorConn = nil end
end

– –– Anti Grab V1 ––
do
local plr = Players.LocalPlayer
local antiGrabConn = nil
local savedCFrame  = nil
local AntiGrabEnabled = false
local HeldConnection  = nil
local isHeld = plr:WaitForChild(“IsHeld”, 10)
if not isHeld then isHeld = Instance.new(“BoolValue”) end
local struggleEvent = ReplicatedStorage:WaitForChild(“CharacterEvents”, 10)
if struggleEvent then struggleEvent = struggleEvent:WaitForChild(“Struggle”, 10) end
if not struggleEvent then struggleEvent = {FireServer = function() end} end

```
local function onHeldChanged(heldState)
    local char = plr.Character
    local hrp  = char and char:FindFirstChild("HumanoidRootPart")
    if heldState then
        if hrp then savedCFrame = hrp.CFrame; hrp.Anchored = true end
        task.spawn(function()
            while isHeld.Value do
                struggleEvent:FireServer(plr)
                task.wait()
            end
            if hrp then
                hrp.Anchored = false
                if savedCFrame then hrp.CFrame = savedCFrame end
            end
        end)
    else
        if hrp then
            hrp.Anchored = false
            if savedCFrame then hrp.CFrame = savedCFrame end
        end
    end
end

antiGrabConn = isHeld.Changed:Connect(onHeldChanged)
if isHeld.Value then onHeldChanged(true) end

plr.CharacterAdded:Connect(function(char)
    task.wait(1)
    local hrp = char:WaitForChild("HumanoidRootPart", 5)
    if hrp and isHeld.Value then onHeldChanged(true) end
end)

local function StopAntiGrab()
    AntiGrabEnabled = false
    if HeldConnection then HeldConnection:Disconnect(); HeldConnection = nil end
    local Root = Player.Character and Player.Character:FindFirstChild("HumanoidRootPart")
    if Root then Root.Anchored = false end
end

local function StartAntiGrab()
    if HeldConnection then HeldConnection:Disconnect(); HeldConnection = nil end
    HeldConnection = BeingHeld:GetPropertyChangedSignal("Value"):Connect(function()
        if not AntiGrabEnabled or not BeingHeld.Value then return end
        local Char = Player.Character
        if not Char then return end
        local Root = Char:FindFirstChild("HumanoidRootPart")
        local Hum  = Char:FindFirstChildOfClass("Humanoid")
        local Head = Char:FindFirstChild("Head")
        local lastFreePos = Root and Root.CFrame
        local renderConn
        task.spawn(function()
            while AntiGrabEnabled and BeingHeld.Value do
                pcall(function()
                    StruggleEvent:FireServer()
                    ReplicatedStorage.CharacterEvents.RagdollRemote:FireServer(Root, 0)
                    ReplicatedStorage.GameCorrectionEvents.StopAllVelocity:FireServer()
                end)
                task.wait()
            end
        end)
        task.spawn(function()
            while AntiGrabEnabled and BeingHeld.Value do
                pcall(function()
                    Hum.Sit = false
                    Hum:ChangeState(Enum.HumanoidStateType.GettingUp)
                    Hum:SetStateEnabled(Enum.HumanoidStateType.Jumping, true)
                    Root.Anchored = true
                    Root.AssemblyLinearVelocity = Vector3.zero
                    Root.AssemblyAngularVelocity = Vector3.zero
                end)
                task.wait()
            end
            if Root then Root.Anchored = false end
            if renderConn then renderConn:Disconnect() end
        end)
        task.spawn(function()
            while AntiGrabEnabled and Root and Root.Anchored do
                pcall(function()
                    if Hum and Hum.MoveDirection.Magnitude > 0 then
                        Root.CFrame = Root.CFrame + (Hum.MoveDirection * 0.2)
                        lastFreePos = Root.CFrame
                    end
                    if Head then
                        Head.CanCollide = false
                        Head.AssemblyLinearVelocity = Vector3.zero
                        Head.AssemblyAngularVelocity = Vector3.zero
                    end
                end)
                task.wait()
            end
        end)
        renderConn = R.RenderStepped:Connect(function()
            if not AntiGrabEnabled or not BeingHeld.Value then renderConn:Disconnect(); return end
            if Root and lastFreePos then
                Root.CFrame = lastFreePos
                Root.AssemblyLinearVelocity = Vector3.zero
                Root.AssemblyAngularVelocity = Vector3.zero
            end
        end)
    end)
end

DefenseGroup:CreateToggle({
    Name     = "Anti Grab",
    Flag     = "Anti Grab",
    Default  = false,
    Callback = function(Value)
        SetToggleState("Anti Grab", Value)
        AntiGrabEnabled = Value
        if Value then StartAntiGrab() else StopAntiGrab() end
    end
})
```

end

– –– Anti Grab V2 (Full Defense) ––
do
local plr = Players.LocalPlayer
local Player2 = plr
local antiGrabConn2 = nil
local savedCFrame2  = nil
local AntiGrabEnabled2 = false
local HeldConnection2  = nil
local isHeld2 = plr:WaitForChild(“IsHeld”, 10)
if not isHeld2 then isHeld2 = Instance.new(“BoolValue”) end
local struggleEvent2 = ReplicatedStorage:WaitForChild(“CharacterEvents”, 10)
if struggleEvent2 then struggleEvent2 = struggleEvent2:WaitForChild(“Struggle”, 10) end
local StruggleEvent2 = struggleEvent2
local BeingHeld2 = isHeld2

```
local function onHeldChanged2(heldState)
    local char = plr.Character
    local hrp  = char and char:FindFirstChild("HumanoidRootPart")
    if heldState then
        if hrp then savedCFrame2 = hrp.CFrame; hrp.Anchored = true end
        task.spawn(function()
            while isHeld2.Value do
                if StruggleEvent2 then StruggleEvent2:FireServer(plr) end
                task.wait()
            end
            if hrp then
                hrp.Anchored = false
                if savedCFrame2 then hrp.CFrame = savedCFrame2 end
            end
        end)
    else
        if hrp then
            hrp.Anchored = false
            if savedCFrame2 then hrp.CFrame = savedCFrame2 end
        end
    end
end

antiGrabConn2 = isHeld2.Changed:Connect(onHeldChanged2)
if isHeld2.Value then onHeldChanged2(true) end

local function StopAntiGrab2()
    AntiGrabEnabled2 = false
    if HeldConnection2 then HeldConnection2:Disconnect(); HeldConnection2 = nil end
    local Root = Player2.Character and Player2.Character:FindFirstChild("HumanoidRootPart")
    if Root then Root.Anchored = false end
    if Player2.Character then
        for _, part in pairs(Player2.Character:GetDescendants()) do
            if part:IsA("BasePart") then part.CanCollide = true end
        end
    end
end

local function StartAntiGrab2()
    if HeldConnection2 then HeldConnection2:Disconnect() end
    HeldConnection2 = BeingHeld2:GetPropertyChangedSignal("Value"):Connect(function()
        if not AntiGrabEnabled2 or not BeingHeld2.Value then return end
        local Char = Player2.Character
        if not Char then return end
        local Root = Char:FindFirstChild("HumanoidRootPart")
        local Hum  = Char:FindFirstChildOfClass("Humanoid")
        local Head = Char:FindFirstChild("Head")
        local lastFreePos = Root and Root.CFrame
        local renderConn2
        for _, part in pairs(Char:GetDescendants()) do
            if part:IsA("BasePart") then part.CanCollide = false end
        end
        task.spawn(function()
            while AntiGrabEnabled2 and BeingHeld2.Value do
                pcall(function()
                    if StruggleEvent2 then StruggleEvent2:FireServer() end
                    ReplicatedStorage.CharacterEvents.RagdollRemote:FireServer(Root, 0)
                    ReplicatedStorage.GameCorrectionEvents.StopAllVelocity:FireServer()
                end)
                task.wait()
            end
        end)
        task.spawn(function()
            while AntiGrabEnabled2 and BeingHeld2.Value do
                pcall(function()
                    Hum.Sit = false
                    Hum:ChangeState(Enum.HumanoidStateType.GettingUp)
                    Hum:SetStateEnabled(Enum.HumanoidStateType.Jumping, true)
                    Root.Anchored = true
                    Root.AssemblyLinearVelocity = Vector3.zero
                    Root.AssemblyAngularVelocity = Vector3.zero
                end)
                task.wait()
            end
            if Root then Root.Anchored = false end
        end)
        task.spawn(function()
            while AntiGrabEnabled2 and Root and Root.Anchored do
                pcall(function()
                    if Hum and Hum.MoveDirection.Magnitude > 0 then
                        Root.CFrame = Root.CFrame + (Hum.MoveDirection * 0.2)
                        lastFreePos = Root.CFrame
                    end
                    if Head then
                        Head.CanCollide = false
                        Head.AssemblyLinearVelocity = Vector3.zero
                        Head.AssemblyAngularVelocity = Vector3.zero
                    end
                end)
                task.wait()
            end
        end)
        renderConn2 = R.RenderStepped:Connect(function()
            if not AntiGrabEnabled2 or not BeingHeld2.Value then renderConn2:Disconnect(); return end
            if Root and lastFreePos then
                Root.CFrame = lastFreePos
                Root.AssemblyLinearVelocity = Vector3.zero
                Root.AssemblyAngularVelocity = Vector3.zero
            end
        end)
    end)
end

DefenseGroup:CreateToggle({
    Name     = "Anti Grab V2 (Full Defense)",
    Flag     = "Anti Grab V2 (Full Defense)",
    Default  = false,
    Callback = function(Value)
        SetToggleState("Anti Grab V2 (Full Defense)", Value)
        AntiGrabEnabled2 = Value
        if Value then StartAntiGrab2() else StopAntiGrab2() end
    end
})

Player2.CharacterAdded:Connect(function(char)
    task.wait(1)
    if AntiGrabEnabled2 and BeingHeld2.Value then StartAntiGrab2() end
end)
```

end

DefenseGroup:CreateToggle({
Name     = “Auto Reset”,
Flag     = “Auto Reset”,
Default  = false,
Callback = function(v)
if _G.AutoResetCon then _G.AutoResetCon:Disconnect() end
if v then
_G.AutoResetCon = game:GetService(“ReplicatedStorage”).GameCorrectionEvents.GameCorrectionsNotify.OnClientEvent:Connect(function(r)
if r == “Flying” then
local char = game:GetService(“Players”).LocalPlayer.Character
local hum  = char and char:FindFirstChildOfClass(“Humanoid”)
if hum then
notify(“Silenced Hub”, “Resetting to prevent ban”, 4)
char:BreakJoints()
hum.Health = 0
end
end
end)
end
end
})

DefenseGroup:CreateToggle({
Name     = “Anti Void”,
Flag     = “Anti Void”,
Default  = false,
Callback = function(v)
SetToggleState(“Anti Void”, v)
workspace.FallenPartsDestroyHeight = v and 0/0 or -100
end
})

local antiBlob1T = false
local function antiBlob1F()
antiBlob1T = true
workspace.DescendantAdded:Connect(function(toy)
if toy.Name == “CreatureBlobman” and antiBlob1T then
toy.LeftDetector:Destroy()
toy.RightDetector:Destroy()
end
end)
end

DefenseGroup:CreateToggle({
Name     = “Anti-Blobman Aura”,
Flag     = “Anti-Blobman Aura”,
Default  = false,
Callback = function(enabled)
SetToggleState(“Anti-Blobman Aura”, enabled)
local OAA_players     = game:GetService(“Players”)
local OAA_localPlayer = game.Players.LocalPlayer
local OAA_antiBlobmanGrabEnabled = enabled
local function OAA_getCharacter(player)
return player.Character or (player.CharacterAdded and player.CharacterAdded:Wait()) or nil
end
local function OAA_getHumanoidRootPart(character) return character:FindFirstChild(“HumanoidRootPart”) or nil end
local function OAA_getHumanoid(character) return character:FindFirstChild(“Humanoid”) or nil end
local function OAA_getDistance(part1, part2) return (part1.Position - part2.Position).Magnitude end
local function OAA_setNetworkOwner(part, cframe)
task.spawn(function()
local setNetworkOwnerRemote = ReplicatedStorage:WaitForChild(“GrabEvents”):WaitForChild(“SetNetworkOwner”)
setNetworkOwnerRemote:FireServer(part, cframe)
end)
end
OAA_antiBlobmanGrabEnabled = enabled
task.spawn(function()
while OAA_antiBlobmanGrabEnabled do
for _, player in pairs(OAA_players:GetPlayers()) do
if player and OAA_getCharacter(player) then
local playerCharacter = OAA_getCharacter(player)
local playerRootPart  = playerCharacter and OAA_getHumanoidRootPart(playerCharacter)
local playerHumanoid  = playerCharacter and OAA_getHumanoid(playerCharacter)
local myCharacter     = OAA_getCharacter(OAA_localPlayer)
local myRootPart      = myCharacter and OAA_getHumanoidRootPart(myCharacter)
if player.Name ~= OAA_localPlayer.Name
and playerRootPart and myRootPart and playerHumanoid
and playerHumanoid.SeatPart
and playerHumanoid.SeatPart.Parent
and playerHumanoid.SeatPart.Parent.Name == “CreatureBlobman”
and OAA_getDistance(playerRootPart, myRootPart) <= 19
then
OAA_setNetworkOwner(playerRootPart, playerRootPart.CFrame)
task.wait(0.01)
end
end
end
task.wait(0.01)
end
end) – end task.spawn
end,
})

local antiExplodeT = false
local function antiExplodeF()
antiExplodeT = true
local char = Player.Character
if not char then return end
local hrp = char:WaitForChild(“HumanoidRootPart”)
workspace.ChildAdded:Connect(function(model)
if model.Name == “Part” and antiExplodeT then
local mag = (model.Position - hrp.Position).Magnitude
if mag <= 20 then
hrp.Anchored = true
wait(0.01)
while char[“Right Arm”] and char[“Right Arm”].RagdollLimbPart.CanCollide do
wait(0.001)
end
hrp.Anchored = false
end
end
end)
end

DefenseGroup:CreateToggle({
Name     = “Anti Explosion”,
Flag     = “Anti Explosion”,
Default  = false,
Callback = function(on)
SetToggleState(“Anti Explosion”, on)
if on then antiExplodeF() else antiExplodeT = false end
end
})

local hookBurnConn
local function hookBurn(char)
pcall(function()
local hum = char:WaitForChild(“Humanoid”)
local hrp = char:WaitForChild(“HumanoidRootPart”)
char.PrimaryPart = hrp
if hookBurnConn then hookBurnConn:Disconnect() end
local fireDebounce = hum:FindFirstChild(“FireDebounce”)
if not fireDebounce then return end
hookBurnConn = fireDebounce.Changed:Connect(function(isBurning)
if isBurning then
local me = char
local oldCF = hrp.CFrame
local plots = workspace:FindFirstChild(“Plots”)
if plots and plots:FindFirstChild(“Plot2”) then
local plot2  = plots.Plot2
local barrier = plot2:FindFirstChild(“Barrier”)
local pb = barrier and barrier:FindFirstChild(“PlotBarrier”)
if pb and pb:IsA(“BasePart”) then
local safeCF = pb.CFrame * CFrame.new(0, 6, 0)
me:SetPrimaryPartCFrame(safeCF)
task.wait(0.3)
local firePart = me:FindFirstChild(“FirePlayerPart”, true)
if firePart then
for _, obj in ipairs(firePart:GetChildren()) do
if obj:IsA(“Sound”) then obj:Stop() end
if obj:IsA(“Light”) or obj:IsA(“ParticleEmitter”) then obj.Enabled = false end
end
if firePart:FindFirstChild(“CanBurn”) then firePart.CanBurn.Value = false end
if hum:FindFirstChild(“FireDebounce”) then hum.FireDebounce.Value = false end
end
task.wait(0.6)
if me and me.PrimaryPart then me:SetPrimaryPartCFrame(oldCF) end
end
end
end
end)
end) – end pcall wrapper
end

DefenseGroup:CreateToggle({
Name     = “Anti Burn”,
Flag     = “Anti Burn”,
Default  = false,
Callback = function(on)
SetToggleState(“Anti Burn”, on)
if on then hookBurn(Player.Character)
elseif hookBurnConn then hookBurnConn:Disconnect() end
end
})

local antiStickyT = false
DefenseGroup:CreateToggle({
Name     = “Anti Sticky”,
Flag     = “Anti Sticky”,
Default  = false,
Callback = function(Value)
SetToggleState(“Anti Sticky”, Value)
antiStickyT = Value
if Player.PlayerScripts:FindFirstChild(“StickyPartsTouchDetection”) then
Player.PlayerScripts.StickyPartsTouchDetection.Disabled = Value
end
end,
})

local plrDef = game:GetService(“Players”).LocalPlayer
local RunService = game:GetService(“RunService”)
if not _G.cons then _G.cons = {} end

DefenseGroup:CreateToggle({
Name     = “Anti Loop Kill”,
Flag     = “Anti Loop Kill”,
Default  = false,
Callback = function(v)
SetToggleState(“Anti Loop Kill”, v)
if _G.cons[“antiloopkill”] then
_G.cons[“antiloopkill”]:Disconnect()
_G.cons[“antiloopkill”] = nil
end
if v then
_G.cons[“antiloopkill”] = plrDef.CharacterAdded:Connect(function(char)
local hrp = char:WaitForChild(“HumanoidRootPart”, 5)
if hrp then
RunService.RenderStepped:Wait()
local target = CFrame.new(524.703979, 93.7120056, -375.040985)
hrp.CFrame = target
for i = 1, 2 do RunService.RenderStepped:Wait(); hrp.CFrame = target end
end
end)
end
end
})

DefenseGroup:CreateToggle({
Name     = “Anti Lag”,
Flag     = “Anti Lag”,
Default  = false,
Callback = function(Value)
SetToggleState(“Anti Lag”, Value)
– Anti-lag grab folder logic preserved from original
end,
})

DefenseGroup:CreateButton({
Name      = “Sequential Double-Reset”,
Flag      = “Sequential Double-Reset”,
Callback  = function()
local lp  = game:GetService(“Players”).LocalPlayer
local rep = game:GetService(“ReplicatedStorage”)
local function killChar(character)
if character then
local hum = character:FindFirstChildOfClass(“Humanoid”)
if hum then hum.Health = 0; hum:ChangeState(Enum.HumanoidStateType.Dead) end
character:BreakJoints()
local struggle = rep:FindFirstChild(“Struggle”, true)
if struggle then struggle:FireServer() end
end
end
notify(“Silenced Hub”, “First Resetting…”, 2)
killChar(lp.Character)
local connection
connection = lp.CharacterAdded:Connect(function(newCharacter)
connection:Disconnect()
task.wait(0.3)
killChar(newCharacter)
notify(“Silenced Hub”, “Double Reset Complete”, 2)
end)
end,
DoubleClick = false,
Tooltip   = “Kills you, waits for respawn, then kills you again instantly.”
})

– –– DefenseExtra toggles ––
DefenseExtra:CreateToggle({
Name     = “Anti Paint”,
Flag     = “Anti Paint”,
Default  = false,
Callback = function(state)
SetToggleState(“Anti Paint”, state)
if state then deleteAllPaintParts(); watchNewPaintParts(); setTouchQuery(false)
else restorePaintParts(); disconnectWatchers(); setTouchQuery(true) end
end
})

local autoGucciActive = false
DefenseExtra:CreateToggle({
Name     = “Anti Gucci (Blobman)”,
Flag     = “Anti Gucci (Blobman)”,
Default  = false,
Callback = function(Value)
SetToggleState(“Anti Gucci (Blobman)”, Value)
autoGucciActive = Value
if Value then
startAntiGucci()
notify(“Silenced Hub”, “Auto Gucci active (monitoring)”, 3)
task.spawn(function()
while autoGucciActive do
local toysFolder = Workspace:FindFirstChild(Player.Name .. “SpawnedInToys”)
local blobExists = toysFolder and toysFolder:FindFirstChild(“CreatureBlobman”)
if not blobExists then
stopAntiGucci(); spawnBlobman()
notify(“Silenced Hub”, “Blobman lost, respawning…”, 3)
local retries = 0
repeat
task.wait(0.2); retries = retries + 1
toysFolder = Workspace:FindFirstChild(Player.Name .. “SpawnedInToys”)
until (toysFolder and toysFolder:FindFirstChild(“CreatureBlobman”)) or retries > 25 or not autoGucciActive
if autoGucciActive and toysFolder and toysFolder:FindFirstChild(“CreatureBlobman”) then
startAntiGucci()
notify(“Silenced Hub”, “Blobman restored.”, 3)
end
end
task.wait(0.5)
end
end)
else
autoGucciActive = false; stopAntiGucci()
notify(“Silenced Hub”, “Auto Gucci disabled.”, 3)
end
end
})

DefenseExtra:CreateToggle({
Name     = “Gucci (Invisible)”,
Flag     = “Gucci(Invisible)”,
Default  = false,
Callback = function(v)
SetToggleState(“Gucci(Invisible)”, v)
if v then
local blobb
local plrG = game.Players.LocalPlayer
local HRP  = plrG.Character:WaitForChild(“HumanoidRootPart”)
pcall(function()
local pal, pal2
pal2 = plrG.PlayerGui.MenuGui.Menu.TabContents.ToyDestroy.Contents.ChildAdded:Connect(function(c)
if c.Name == “TractorGreen” then pal = c; task.wait(); pal2:Disconnect(); pal2 = nil end
end)
spawn(function()
task.wait(1)
if pal and pal:FindFirstChild(“ViewItemButton”) then
local mess = pal.ViewItemButton:FindFirstChild(“NewMessage”)
if mess then
local clonedMess = mess:Clone()
clonedMess.Name = “Anti Gucci”
clonedMess.TextColor3 = Color3.fromRGB(255, 255, 255)
clonedMess.Visible = true
clonedMess.Parent = pal.ViewItemButton
end
end
end)
end)
game:GetService(“ReplicatedStorage”).MenuToys.SpawnToyRemoteFunction:InvokeServer(“TractorGreen”, HRP.CFrame * CFrame.new(5, 5, 20), Vector3.new())
local inv = workspace:WaitForChild(plrG.Name .. “SpawnedInToys”)
blobb = inv:WaitForChild(“TractorGreen”, 3)
if blobb then
blobb.Name = “tractorgucci”
blobb:WaitForChild(“VehicleSeat”, 3):Sit(plrG.Character.Humanoid)
task.spawn(function()
local endTime = tick() + 3
while tick() < endTime do
game:GetService(“ReplicatedStorage”).CharacterEvents.RagdollRemote:FireServer(HRP, 0)
task.wait()
end
end)
task.wait()
while blobb.VehicleSeat.Occupant ~= plrG.Character.Humanoid do task.wait() end
plrG.Character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
pcall(function() game:GetService(“ReplicatedStorage”).GrabEvents.SetNetworkOwner:FireServer(blobb.Part, blobb.Part.CFrame) end)
task.wait(0.2)
blobb.VehicleSeat.CFrame = CFrame.new(0, 0/0, 0)
end
else
local plrG = game.Players.LocalPlayer
local inv  = workspace:FindFirstChild(plrG.Name .. “SpawnedInToys”)
if inv and inv:FindFirstChild(“tractorgucci”) then
game:GetService(“ReplicatedStorage”).MenuToys.DestroyToy:FireServer(inv.tractorgucci)
end
local hum = plrG.Character and plrG.Character:FindFirstChild(“Humanoid”)
if hum then
for i = 1, 30 do hum.Sit = true; task.wait(); hum.Sit = false end
end
end
end
})

local autoGucciActiveTrain = false
DefenseExtra:CreateToggle({
Name     = “Anti Gucci (Train)”,
Flag     = “Anti Gucci (Train)”,
Default  = false,
Callback = function(Value)
SetToggleState(“Anti Gucci (Train)”, Value)
autoGucciActiveTrain = Value
if Value then
startAntiGucciTrain()
notify(“Silenced Hub”, “Train Gucci active (monitoring)”, 3)
task.spawn(function()
while autoGucciActiveTrain do
local trainFolder = workspace.Map.AlwaysHereTweenedObjects
local trainExists  = trainFolder and trainFolder:FindFirstChild(“Train”)
if not trainExists then
stopAntiGucciTrain()
notify(“Silenced Hub”, “Train lost, waiting…”, 3)
local retries = 0
repeat
task.wait(0.2); retries = retries + 1
trainFolder = workspace.Map.AlwaysHereTweenedObjects
until (trainFolder and trainFolder:FindFirstChild(“Train”)) or retries > 25 or not autoGucciActiveTrain
if autoGucciActiveTrain and trainFolder and trainFolder:FindFirstChild(“Train”) then
startAntiGucciTrain()
notify(“Silenced Hub”, “Train restored.”, 3)
end
end
task.wait(0.5)
end
end)
else
autoGucciActiveTrain = false; stopAntiGucciTrain()
notify(“Silenced Hub”, “Train Gucci disabled.”, 3)
end
end
})

DefenseExtra:CreateButton({
Name     = “Delete Legs”,
Flag     = “Delete Legs”,
Callback = function()
local char = Player.Character
if not char then return end
if char:FindFirstChild(“Left Leg”) and char:FindFirstChild(“Right Leg”) then
local ll   = char:FindFirstChild(“Left Leg”)
local rl   = char:FindFirstChild(“Right Leg”)
local void = workspace.FallenPartsDestroyHeight
local pos  = char.Torso.CFrame
workspace.FallenPartsDestroyHeight = -100
ReplicatedStorage.CharacterEvents.RagdollRemote:FireServer(char.HumanoidRootPart, 2)
task.wait(0.5)
rl.CFrame = CFrame.new(0, -10000, 0)
ll.CFrame = CFrame.new(0, -10000, 0)
task.wait(0.3)
char.Torso.CFrame = CFrame.new(0, -9970, 0)
task.wait(0.5)
char.Torso.CFrame = pos
task.wait(0.5)
workspace.FallenPartsDestroyHeight = void
end
end
})

– Toy list for Anti Input Lag
local ToyList = {
[“Coconut”]=“FoodCoconut”,[“Banana”]=“FoodBanana”,[“Fries”]=“FoodFrenchFries”,
[“MeatStick”]=“FoodMeatStick”,[“Poop”]=“PoopPile”,[“Donut”]=“FoodDonut”,
[“Cake”]=“FoodCakePink”,[“Burger”]=“FoodHamburger”,[“Pizza”]=“FoodPizzaCheese”,
[“Hotdog”]=“FoodHotdog”,[“Mushroom”]=“FoodMushroomPoison”,[“Banjo”]=“InstrumentGuitarBanjo”,
[“Violin”]=“InstrumentGuitarViolin”,[“Ukulele”]=“InstrumentGuitarUkulele”,
[“Sax”]=“InstrumentWoodwindSaxophone”,[“Vuvuzela”]=“InstrumentBrassVuvuzela”,
[“Bongos”]=“InstrumentDrumBongos”,[“Mic”]=“InstrumentVoiceMicrophone”,
[“Pepperoni”]=“FoodPizzaPepperoni”,[“Piano”]=“InstrumentPianoMelodica”,
[“Bread”]=“FoodBread”,[“Egg”]=“FoodDippyEgg”,[“Mayo”]=“FoodMayonnaise”,
[“WhiteMug”]=“CupMugWhite”,[“Ocarina”]=“InstrumentWoodwindOcarina”,
[“SparklePoop”]=“PoopPileSparkle”,[“BrownMug”]=“CupMugBrown”,
[“Trumpet”]=“InstrumentBrassTrumpet”,[“Snare”]=“InstrumentDrumSnare”,[“Lyre”]=“InstrumentGuitarLyre”,
}
local DropdownValues = {}
for shortName, _ in pairs(ToyList) do table.insert(DropdownValues, shortName) end
table.sort(DropdownValues)
local SelectedToy = ToyList[DropdownValues[1]]

DefenseExtra:CreateDropdown({
Name     = “Input Lag Item”,
Flag     = “Input Lag Item”,
Items    = DropdownValues,
Default  = “Q”,
Callback = function(Value) SelectedToy = ToyList[Value] end
})

DefenseExtra:CreateToggle({
Name     = “Anti Input Lag”,
Flag     = “Anti Input Lag”,
Default  = false,
Callback = function(Value)
SetToggleState(“Anti Input Lag”, Value)
_G.AntiInputLag = Value
if not Value then return end
task.spawn(function()
local plrAIL = Players.LocalPlayer
local charAIL = plrAIL.Character or plrAIL.CharacterAdded:Wait()
local hrpAIL  = charAIL:WaitForChild(“HumanoidRootPart”)
local SpawnRemote = ReplicatedStorage:WaitForChild(“MenuToys”):WaitForChild(“SpawnToyRemoteFunction”)
local SelectedToyName = SelectedToy
while _G.AntiInputLag do
local toysFolder = workspace:FindFirstChild(plrAIL.Name .. “SpawnedInToys”)
if not toysFolder then R.Heartbeat:Wait(); continue end
local toy = toysFolder:FindFirstChild(SelectedToyName)
if not toy then
pcall(function() SpawnRemote:InvokeServer(SelectedToyName, hrpAIL.CFrame * CFrame.new(0, 5, 0), Vector3.zero) end)
R.Heartbeat:Wait()
else
local holdPart = toy:FindFirstChild(“HoldPart”)
if holdPart then
local holdingPlayer = holdPart:FindFirstChild(“HoldingPlayer”) and holdPart.HoldingPlayer.Value
if holdingPlayer and holdingPlayer ~= plrAIL then
pcall(function() holdPart.DropItemRemoteFunction:InvokeServer(toy, hrpAIL.CFrame * CFrame.new(0, 2000, 0), Vector3.zero) end)
toy:Destroy()
else
while _G.AntiInputLag do
pcall(function()
holdPart.HoldItemRemoteFunction:InvokeServer(toy, charAIL)
holdPart.DropItemRemoteFunction:InvokeServer(toy, hrpAIL.CFrame * CFrame.new(0, 5000, 0), Vector3.zero)
end)
if not toy or not toy.Parent or not toysFolder:FindFirstChild(SelectedToyName) then break end
R.Heartbeat:Wait()
end
end
else
pcall(function()
if toy and toy.Parent then toy:Destroy() end
SpawnRemote:InvokeServer(SelectedToyName, hrpAIL.CFrame * CFrame.new(0, 5, 0), Vector3.zero)
end)
R.Heartbeat:Wait()
end
end
end
end)
end
})

– Auto PCLD Anti Kick
local antiKickPCLDActive = false
local currentGold = nil
local activePCLDLoop = false
local fixedPCLD = nil

local function autoScanPCLD()
local nearest = nil; local shortestDist = math.huge
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if not hrp then return nil end
for _, v in pairs(workspace:GetChildren()) do
if v.Name:find(“PlayerCharacterLocationDetector”) and v:IsA(“BasePart”) then
local dist = (v.Position - hrp.Position).Magnitude
if dist < shortestDist then shortestDist = dist; nearest = v end
end
end
fixedPCLD = nearest; return nearest
end

DefenseExtra:CreateToggle({
Name     = “Auto PCLD Anti Kick”,
Flag     = “AutoPCLDAntiKick”,
Default  = false,
Callback = function(Value)
SetToggleState(“AutoPCLDAntiKick”, Value)
antiKickPCLDActive = Value
if not Value then
activePCLDLoop = false
if currentGold then
pcall(function() ReplicatedStorage.MenuToys.DestroyToy:FireServer(currentGold) end)
currentGold = nil
end
return
end
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if hrp then
fixedPCLD = autoScanPCLD()
if not fixedPCLD then
Library:Notify({Title = “Silenced Hub”, Content = “Error: No PCLD found!”, Duration = 4})
return
end
task.spawn(function()
local spawnPos = fixedPCLD.CFrame * CFrame.new(0, 2, 0)
local MenuToys    = ReplicatedStorage:WaitForChild(“MenuToys”)
local SpawnRemote = MenuToys:WaitForChild(“SpawnToyRemoteFunction”)
SpawnRemote:InvokeServer(“SprayCanWD”, spawnPos, Vector3.zero)
local folderName = Player.Name .. “SpawnedInToys”
local toysFolder = workspace:WaitForChild(folderName, 5)
currentGold = toysFolder and toysFolder:WaitForChild(“SprayCanWD”, 5)
if currentGold then
local holdPart = currentGold:WaitForChild(“HoldPart”, 5)
for _, p in pairs(currentGold:GetDescendants()) do
if p:IsA(“BasePart”) then
p.Transparency = 0.5
p.Color = Color3.fromRGB(0, 210, 255)
p.Material = Enum.Material.Neon
p.CanCollide = false
end
end
pcall(function()
holdPart.HoldItemRemoteFunction:InvokeServer(currentGold, char)
task.wait(0.3)
holdPart.DropItemRemoteFunction:InvokeServer(currentGold, fixedPCLD.CFrame, Vector3.zero)
end)
activePCLDLoop = true
Library:Notify({Title = “Silenced Hub”, Content = “Anti-Kick: Active”, Duration = 3})
while activePCLDLoop and antiKickPCLDActive and currentGold and fixedPCLD do
currentGold:PivotTo(fixedPCLD.CFrame)
local main = currentGold:FindFirstChild(“Main”) or holdPart
if main and main:IsA(“BasePart”) then
main.AssemblyLinearVelocity = Vector3.zero
main.AssemblyAngularVelocity = Vector3.zero
end
RunService.Heartbeat:Wait()
end
else
Library:Notify({Title = “Silenced Hub”, Content = “Failed to spawn item.”, Duration = 3})
end
end)
end
end
})

local tpActive = false
DefenseExtra:CreateToggle({
Name     = “Anti Kick”,
Flag     = “Anti Kick”,
Default  = false,
Callback = function(Value)
SetToggleState(“Anti Kick”, Value)
_G.ShurikenAntiKick = Value

```
    local function ClearKunai()
        local plr = game.Players.LocalPlayer
        local inv = workspace:FindFirstChild(plr.Name .. "SpawnedInToys")
        local destroyrem = game.ReplicatedStorage:FindFirstChild("MenuToys") and game.ReplicatedStorage.MenuToys:FindFirstChild("DestroyToy")
        if inv and destroyrem then
            for _, v in pairs(inv:GetChildren()) do
                if v.Name == "AntiKick" or v.Name == "NinjaShuriken" then
                    pcall(function() destroyrem:FireServer(v) end)
                end
            end
        end
    end

    if Value then
        task.spawn(function()
            local plr = game.Players.LocalPlayer
            local RS2 = game:GetService("ReplicatedStorage")
            local setOwner    = RS2:WaitForChild("GrabEvents"):WaitForChild("SetNetworkOwner")
            local stickyEvent = RS2:WaitForChild("PlayerEvents"):WaitForChild("StickyPartEvent")
            local spawnRemote = RS2.MenuToys.SpawnToyRemoteFunction
            local destroyrem  = RS2:WaitForChild("MenuToys"):WaitForChild("DestroyToy")
            local canSpawn    = plr:WaitForChild("CanSpawnToy")

            local function getHRP()
                if plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                    return plr.Character.HumanoidRootPart
                else
                    local character = plr.CharacterAdded:Wait()
                    return character:WaitForChild("HumanoidRootPart")
                end
            end

            local function CheckForHome()
                if not workspace.PlotItems.PlayersInPlots:FindFirstChild(plr.Name) then
                    return false
                end
                for _, v in pairs(workspace.Plots:GetChildren()) do
                    local sign = v:FindFirstChild("PlotSign")
                    local owners = sign and sign:FindFirstChild("ThisPlotsOwners")
                    if owners then
                        for _, b in pairs(owners:GetChildren()) do
                            if b.Value == plr.Name then
                                local folder = workspace.PlotItems:FindFirstChild(v.Name)
                                if folder then
                                    return true, folder
                                end
                            end
                        end
                    end
                end
                return false
            end

            local function StickKunai(kunai)
                if not kunai or not kunai:FindFirstChild("StickyPart") then return end
                local currentHRP = getHRP()
                if not currentHRP then return end
                if kunai:FindFirstChild("SoundPart") then
                    if not kunai.SoundPart:FindFirstChild("PartOwner") or kunai.SoundPart.PartOwner.Value ~= plr.Name then
                        setOwner:FireServer(kunai.SoundPart, kunai.SoundPart.CFrame)
                    end
                end
                local firePart = currentHRP:FindFirstChild("FirePlayerPart") or currentHRP:WaitForChild("FirePlayerPart", 5)
                if firePart then
                    stickyEvent:FireServer(
                        kunai.StickyPart,
                        firePart,
                        CFrame.new(0, 0, 0) * CFrame.Angles(0, math.rad(90), math.rad(90))
                    )
                end
                -- Apply correct visibility/collision per part name, no collision with local player
                for _, obj in pairs(kunai:GetChildren()) do
                    if obj.Name == "Pyramid" then
                        obj.CanTouch    = false
                        obj.CanCollide  = false
                        obj.CanQuery    = false
                        obj.Transparency = 0
                        if not obj:FindFirstChild("Highlight") then
                            local high = Instance.new("Highlight", obj)
                            high.FillColor = Color3.fromRGB(0, 0, 0)
                        end
                    elseif obj.Name == "Main" then
                        obj.CanTouch    = false
                        obj.CanCollide  = false
                        obj.CanQuery    = false
                        obj.Transparency = 0
                        if not obj:FindFirstChild("Highlight") then
                            local high = Instance.new("Highlight", obj)
                            high.FillColor = Color3.fromRGB(255, 255, 255)
                        end
                    elseif obj:IsA("BasePart") then
                        obj.CanTouch    = false
                        obj.CanCollide  = false
                        obj.CanQuery    = false
                        obj.Transparency = 1
                    end
                end
            end

            local function SpawnToy(name)
                local t = tick()
                while not canSpawn.Value do
                    if not _G.ShurikenAntiKick or tick() - t > 5 then return nil end
                    task.wait(0.1)
                end
                local currentHRP = getHRP()
                if currentHRP then
                    task.spawn(function()
                        pcall(function()
                            spawnRemote:InvokeServer(name, currentHRP.CFrame * CFrame.new(0, 12, 20), Vector3.new(0, 0, 0))
                        end)
                    end)
                end
                local boolik, house = CheckForHome()
                local inv = workspace:FindFirstChild(plr.Name .. "SpawnedInToys")
                if boolik and house then
                    return house:WaitForChild(name, 2)
                elseif not workspace.PlotItems.PlayersInPlots:FindFirstChild(plr.Name) and inv then
                    return inv:WaitForChild(name, 2)
                end
                return nil
            end

            while _G.ShurikenAntiKick do
                task.wait(0.005)
                if not plr.Character or not plr.Character:FindFirstChild("Humanoid") or plr.Character.Humanoid.Health <= 0 then
                    continue
                end
                local inv = workspace:FindFirstChild(plr.Name .. "SpawnedInToys")
                local kunai = inv and inv:FindFirstChild("NinjaShuriken")

                -- Plot/home handling
                if workspace.PlotItems.PlayersInPlots:FindFirstChild(plr.Name) then
                    local boolik, house = CheckForHome()
                    if boolik and house and workspace.Plots:FindFirstChild(house.Name) then
                        local sign = workspace.Plots[house.Name]:FindFirstChild("PlotSign")
                        if sign and sign.ThisPlotsOwners.Value.TimeRemainingNum.Value > 89 then
                            kunai = SpawnToy("NinjaShuriken")
                            if kunai == nil then continue end
                            kunai.Name = "AntiKick"
                            StickKunai(kunai)
                        end
                    end
                end

                if not kunai then
                    if workspace.PlotItems.PlayersInPlots:FindFirstChild(plr.Name) then
                        continue
                    end
                    kunai = SpawnToy("NinjaShuriken")
                    if kunai == nil then continue end
                    kunai.Name = "AntiKick"
                    if not kunai then continue end
                end

                -- Stick loop — keep sticking until kunai is properly welded
                repeat
                    if kunai and kunai:FindFirstChild("StickyPart") and kunai.StickyPart.CanTouch == true then
                        StickKunai(kunai)
                        kunai.Name = "AntiKick"
                    end
                    task.wait(0.3)
                until not kunai
                    or not _G.ShurikenAntiKick
                    or not kunai:FindFirstChild("StickyPart")
                    or kunai.StickyPart.CanTouch == false
                    or not plr.Character
                    or not plr.Character:FindFirstChild("HumanoidRootPart")
                    or (plr.Character.HumanoidRootPart.Position - kunai.StickyPart.Position).Magnitude >= 20

                -- If kunai drifted or is gone, clear it
                if not kunai
                    or not kunai:FindFirstChild("StickyPart")
                    or not plr.Character
                    or not plr.Character:FindFirstChild("HumanoidRootPart")
                    or (plr.Character.HumanoidRootPart.Position - kunai.StickyPart.Position).Magnitude >= 20
                then
                    ClearKunai()
                end

                -- Wait until StickyWeld detaches (kunai removed from body)
                pcall(function()
                    repeat
                        task.wait(0.05)
                    until not _G.ShurikenAntiKick
                        or not plr.Character
                        or not plr.Character:FindFirstChild("Humanoid")
                        or not kunai
                        or not kunai:FindFirstChild("StickyPart")
                        or not kunai.StickyPart:FindFirstChild("StickyWeld")
                        or not kunai.StickyPart.StickyWeld.Part1
                    if not kunai
                        or not kunai:FindFirstChild("StickyPart")
                        or (plr.Character and plr.Character:FindFirstChild("Humanoid") and plr.Character.Humanoid.Health <= 0)
                        or not kunai.StickyPart:FindFirstChild("StickyWeld")
                        or not kunai.StickyPart.StickyWeld.Part1
                    then
                        ClearKunai()
                    end
                end)
            end
        end)
    else
        _G.ShurikenAntiKick = false
        ClearKunai()
    end
end
```

})

– Auto-respawn: re-engage anti kick after character respawn
Player.CharacterAdded:Connect(function()
if _G.ShurikenAntiKick then
task.wait(1)
– The while loop naturally picks up the new HRP via getHRP()
end
end)

DefenseExtra:CreateToggle({
Name     = “Loop TP”,
Flag     = “Loop TP”,
Default  = false,
Callback = function(Value)
SetToggleState(“Loop TP”, Value)
tpActive = Value
local char = Player.Character or Player.CharacterAdded:Wait()
local hrp  = char:WaitForChild(“HumanoidRootPart”)
local hum  = char:FindFirstChildOfClass(“Humanoid”)
if Value then
if hum then hum.PlatformStand = true end
task.spawn(function()
while tpActive and hrp do
hrp.CFrame = CFrame.new(math.random(-500,500), math.random(30,480), math.random(-500,500))
task.wait(0.03)
end
end)
else
if hum then hum.PlatformStand = false end
end
end,
})

– ============================================================
–  TARGET TAB
– ============================================================
local TargetGroup    = Tabs.Target:CreateBlock({Name = “Silenced”, Side = “Left”})
local BlobGroup      = Tabs.Target:CreateBlock({Name = “Silenced”, Side = “Right”})
local BlobHandGroup  = Tabs.Target:CreateBlock({Name = “Silenced”, Side = “Right”})
local WhitelistGroup = Tabs.Target:CreateBlock({Name = “Silenced”, Side = “Right”})

local selectedKickPlayer  = nil
local kickLoopEnabled     = false
local kickLoopConnection  = nil
local savedKickPos        = nil
local currentKickTargetChar = nil
local loopKickDualActive  = false
local blobUseLeftHand     = true
local blobUseRightHand    = true

local function getPlayerList()
local list = {}
for _, plr in ipairs(PS:GetPlayers()) do
if plr ~= Player then
table.insert(list, plr.DisplayName .. “ (@” .. plr.Name .. “)”)
end
end
return list
end

local function getPlayerFromSelection(selection)
if not selection or selection == “” then return nil end
local username = selection:match(”@(.-)%)”)
if username then return PS:FindFirstChild(username) end
return nil
end

– Helper: get or spawn blobman regardless of where player is (home/world)
local function getOrSpawnBlob()
local folder = workspace:FindFirstChild(Player.Name .. “SpawnedInToys”)
local blob   = folder and folder:FindFirstChild(“CreatureBlobman”)
if not blob then
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if hrp then
pcall(function()
RS.MenuToys.SpawnToyRemoteFunction:InvokeServer(
“CreatureBlobman”,
hrp.CFrame * CFrame.new(0, 5, 5),
Vector3.zero
)
end)
local t0 = tick()
repeat
R.Heartbeat:Wait()
folder = workspace:FindFirstChild(Player.Name .. “SpawnedInToys”)
blob   = folder and folder:FindFirstChild(“CreatureBlobman”)
until blob or tick() - t0 > 5
end
end
return blob
end

– Helper: sit local player in blob no matter what
local function ensureSitInBlob()
local blob = getOrSpawnBlob()
if not blob then return false end
local char = Player.Character
local hum  = char and char:FindFirstChildOfClass(“Humanoid”)
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if not hum or not hrp then return false end
if hum.SeatPart and hum.SeatPart.Parent == blob then return true end
local seat = blob:FindFirstChildWhichIsA(“VehicleSeat”)
if seat then
local t = tick()
repeat
hrp.CFrame = seat.CFrame + Vector3.new(0, 1, 0)
hrp.Velocity = Vector3.zero
seat:Sit(hum)
R.Heartbeat:Wait()
until (hum.SeatPart == seat) or (tick() - t > 3)
end
return hum.SeatPart and hum.SeatPart.Parent == blob
end

local PlayerDropdown = TargetGroup:CreateDropdown({
Name     = “Select player for kick”,
List     = getPlayerList(),
Default  = nil,
Callback = function(Value)
selectedKickPlayer = getPlayerFromSelection(Value)
end,
})

local function updateDropdown()
local newList = getPlayerList()
if PlayerDropdown then PlayerDropdown:Refresh(newList, false) end
if selectedKickPlayer and not selectedKickPlayer.Parent then selectedKickPlayer = nil end
end

PS.PlayerAdded:Connect(updateDropdown)
PS.PlayerRemoving:Connect(updateDropdown)
updateDropdown()
local addedConn   = PS.PlayerAdded:Connect(updateDropdown)
local removedConn = PS.PlayerRemoving:Connect(updateDropdown)
Player.CharacterRemoving:Connect(function()
pcall(function() addedConn:Disconnect() end)
pcall(function() removedConn:Disconnect() end)
end)

local customKickHeight = 25
local kickLoopEnabled  = false

TargetGroup:CreateInput({
Name        = “Custom Kick Height”,
Flag        = “Custom Kick Height”,
Default     = “25”,
Placeholder = “Enter height (e.g. 50)”,
Numeric     = true,
Finished    = true,
Callback    = function(Value)
local num = tonumber(Value)
customKickHeight = num or 25
end
})

– –– Blob Hand Options ––
BlobHandGroup:CreateToggle({
Name     = “Left Blob Hand”,
Flag     = “Left Blob Hand”,
Default  = true,
Callback = function(Value)
SetToggleState(“Left Blob Hand”, Value)
blobUseLeftHand = Value
end
})

BlobHandGroup:CreateToggle({
Name     = “Right Blob Hand”,
Flag     = “Right Blob Hand”,
Default  = true,
Callback = function(Value)
SetToggleState(“Right Blob Hand”, Value)
blobUseRightHand = Value
end
})

– –– Auto Sit Blobman ––
do
BlobGroup:CreateToggle({
Name     = “Auto Sit Blobman”,
Flag     = “Auto Sit Blobman”,
Default  = false,
Callback = function(Value)
SetToggleState(“Auto Sit Blobman”, Value)
if Value then
task.spawn(function()
while GetToggleState(“Auto Sit Blobman”) do
local Char = Player.Character
local Hum  = Char and Char:FindFirstChildOfClass(“Humanoid”)
local Root = Char and Char:FindFirstChild(“HumanoidRootPart”)
if Hum and Root and not Hum.SeatPart then
ensureSitInBlob()
end
task.wait(0.1)
end
end)
end
end
})
end

– –– XOCU (grab + blob) ––
BlobGroup:CreateToggle({
Name     = “XOCU (grab + blob)”,
Flag     = “XOCU (grab + blob)”,
Default  = false,
Callback = function(on)
SetToggleState(“XOCU (grab + blob)”, on)
kickLoopEnabled    = on
loopKickDualActive = on
if on then
if not selectedKickPlayer then
notify(“Error”, “Select target first”, 3)
return
end
task.spawn(function()
local GE = RS:FindFirstChild(“GrabEvents”)
– Check we are already in a blob — do not force sit
local initChar = Player.Character
local initSeat = initChar and initChar.Humanoid and initChar.Humanoid.SeatPart
if not initSeat or initSeat.Parent.Name ~= “CreatureBlobman” then
notify(“XOCU”, “You must be sitting in a blob first!”, 3)
kickLoopEnabled = false; loopKickDualActive = false; return
end
local myChar = Player.Character
local myRoot = myChar and myChar:FindFirstChild(“HumanoidRootPart”)
if not myRoot then kickLoopEnabled = false; loopKickDualActive = false; return end

```
            -- Save blob's position at start — blob stays here the whole time
            local seat0   = initChar and initChar.Humanoid and initChar.Humanoid.SeatPart
            local blob0   = seat0 and seat0.Parent
            local blobHRP = blob0 and (blob0:FindFirstChild("HumanoidRootPart") or blob0.PrimaryPart)
            local blobAnchorCF = blobHRP and blobHRP.CFrame or myRoot.CFrame

            while kickLoopEnabled do
                local target = selectedKickPlayer
                if not target or not target.Parent or not target.Character then break end
                local tChar = target.Character
                local tRoot = tChar:FindFirstChild("HumanoidRootPart")
                local tHum  = tChar:FindFirstChild("Humanoid")

                -- Check still in blob — don't move if not
                local seat = myChar and myChar.Humanoid and myChar.Humanoid.SeatPart
                local inBlob = seat and seat.Parent and seat.Parent.Name == "CreatureBlobman"
                if not inBlob then
                    R.Heartbeat:Wait()
                    continue
                end

                local blobman = seat.Parent
                -- Keep blob anchored in place — move target to blob, not blob to target
                local blobHRPNow = blobman:FindFirstChild("HumanoidRootPart") or blobman.PrimaryPart
                if blobHRPNow then
                    pcall(function()
                        blobHRPNow.CFrame = blobAnchorCF
                        blobHRPNow.AssemblyLinearVelocity = Vector3.zero
                        blobHRPNow.AssemblyAngularVelocity = Vector3.zero
                    end)
                end

                if tRoot and tHum and tHum.Health > 0 then
                    local remoteFolder = blobman:FindFirstChild("BlobmanSeatAndOwnerScript")
                    local grab = remoteFolder and remoteFolder:FindFirstChild("CreatureGrab")
                    local drop = remoteFolder and remoteFolder:FindFirstChild("CreatureDrop")
                    local L_Det  = blobman:FindFirstChild("LeftDetector")
                    local R_Det  = blobman:FindFirstChild("RightDetector")
                    local L_Weld = L_Det and (L_Det:FindFirstChild("LeftWeld") or L_Det:FindFirstChild("RigidConstraint"))
                    local R_Weld = R_Det and (R_Det:FindFirstChild("RightWeld") or R_Det:FindFirstChild("RigidConstraint"))

                    -- Bring the target to the blob's position (not the other way)
                    if GE then
                        pcall(function()
                            tHum.PlatformStand = true
                            GE.SetNetworkOwner:FireServer(tRoot, tRoot.CFrame)
                        end)
                    end
                    -- Teleport target to blob location
                    local targetLockPos = blobAnchorCF * CFrame.new(0, customKickHeight, 0)
                    pcall(function()
                        tRoot.CFrame = targetLockPos
                        tRoot.AssemblyLinearVelocity = Vector3.zero
                        tRoot.Velocity = Vector3.zero
                    end)

                    if grab and drop then
                        pcall(function()
                            if blobUseLeftHand  and L_Det and L_Weld then grab:FireServer(L_Det, tRoot, L_Weld); drop:FireServer(L_Weld, tRoot) end
                            if blobUseRightHand and R_Det and R_Weld then grab:FireServer(R_Det, tRoot, R_Weld); drop:FireServer(R_Weld, tRoot) end
                        end)
                    end

                    if GE then
                        pcall(function()
                            GE.DestroyGrabLine:FireServer(tRoot)
                            GE.CreateGrabLine:FireServer(tRoot, Vector3.zero, tRoot.Position, false)
                        end)
                    end
                end
                R.Heartbeat:Wait()
            end
            kickLoopEnabled = false; loopKickDualActive = false
        end)
    else
        kickLoopEnabled = false; loopKickDualActive = false
    end
end
```

})

– –– XOCU spam blob loop ––
BlobGroup:CreateToggle({
Name     = “XOCU spam blob loop”,
Flag     = “XOCU spam blob loop”,
Default  = false,
Callback = function(on)
SetToggleState(“XOCU spam blob loop”, on)
loopKickDualActive = on
if on then
if not selectedKickPlayer then
notify(“Error”, “Select target first”, 3)
return
end
task.spawn(function()
– Only operate if already in a blob — don’t force sit
local initChar = Player.Character
local initSeat = initChar and initChar.Humanoid and initChar.Humanoid.SeatPart
if not initSeat or initSeat.Parent.Name ~= “CreatureBlobman” then
notify(“XOCU Blob”, “You must be sitting in a blob first!”, 3)
loopKickDualActive = false; return
end
while loopKickDualActive do
local target = selectedKickPlayer
local char   = Player.Character
local seat   = char and char.Humanoid and char.Humanoid.SeatPart

```
                -- Only operate if already in a blob — don't force-sit
                if not seat or seat.Parent.Name ~= "CreatureBlobman" then task.wait(0.5); continue end
                if not target or not target.Character then task.wait(0.5); continue end

                local blobman      = seat.Parent
                local remoteFolder = blobman:FindFirstChild("BlobmanSeatAndOwnerScript")
                local grab         = remoteFolder and remoteFolder:FindFirstChild("CreatureGrab")
                local drop         = remoteFolder and remoteFolder:FindFirstChild("CreatureDrop")
                local targetHRP    = target.Character:FindFirstChild("HumanoidRootPart")
                local L_Det  = blobman:FindFirstChild("LeftDetector")
                local R_Det  = blobman:FindFirstChild("RightDetector")
                local L_Weld = L_Det and (L_Det:FindFirstChild("LeftWeld") or L_Det:FindFirstChild("RigidConstraint"))
                local R_Weld = R_Det and (R_Det:FindFirstChild("RightWeld") or R_Det:FindFirstChild("RigidConstraint"))

                if targetHRP and grab and drop then
                    pcall(function()
                        if blobUseLeftHand  and L_Det and L_Weld then grab:FireServer(L_Det, targetHRP, L_Weld); drop:FireServer(L_Weld, targetHRP) end
                        if blobUseRightHand and R_Det and R_Weld then grab:FireServer(R_Det, targetHRP, R_Weld); drop:FireServer(R_Weld, targetHRP) end
                    end)
                end
                task.wait()
            end
        end)
    else
        loopKickDualActive = false
    end
end
```

})

– –– XOCU Kill Blob [Fast] ––
BlobGroup:CreateToggle({
Name     = “XOCU Kill Blob [Fast]”,
Flag     = “XOCU Kill Blob [Fast]”,
Default  = false,
Callback = function(Value)
SetToggleState(“XOCU Kill Blob [Fast]”, Value)
_G.LoopKillBlobActive = Value
if Value then
local tt = selectedKickPlayer
if not tt or not tt.Character or not tt.Character:FindFirstChild(“HumanoidRootPart”) then
notify(“Error”, “Select a valid target first!”, 3)
return
end
task.spawn(function()
while _G.LoopKillBlobActive do
pcall(function()
local char = Player.Character
local seat = char and char.Humanoid and char.Humanoid.SeatPart
if not seat or seat.Parent.Name ~= “CreatureBlobman” then return end – not in blob, skip
local Blob = seat.Parent
if Blob and Blob.Name == “CreatureBlobman” then
local remotes = Blob:FindFirstChild(“BlobmanSeatAndOwnerScript”)
local CG = remotes and remotes:FindFirstChild(“CreatureGrab”)
local CD = remotes and (remotes:FindFirstChild(“CreatureRelease”) or remotes:FindFirstChild(“CreatureDrop”))
local weld
if blobUseRightHand and Blob.RightDetector then
weld = Blob.RightDetector:FindFirstChild(“RightWeld”) or Blob.RightDetector:FindFirstChild(“RigidConstraint”)
elseif blobUseLeftHand and Blob.LeftDetector then
weld = Blob.LeftDetector:FindFirstChild(“LeftWeld”) or Blob.LeftDetector:FindFirstChild(“RigidConstraint”)
end
local bHRP = Blob:FindFirstChild(“HumanoidRootPart”) or Blob.PrimaryPart
local blobAnchorPos = bHRP and bHRP.CFrame  – keep blob here

```
                        if tt.Character and tt.Character:FindFirstChild("HumanoidRootPart") and tt.Character.Humanoid.Health > 0 then
                            local ttRoot = tt.Character.HumanoidRootPart
                            -- Bring target to blob, not blob to target
                            pcall(function()
                                RS.GrabEvents.SetNetworkOwner:FireServer(ttRoot, ttRoot.CFrame)
                                ttRoot.CFrame = blobAnchorPos * CFrame.new(0, 3, 0)
                                ttRoot.AssemblyLinearVelocity = Vector3.zero
                            end)
                            -- Keep blob locked in place
                            if bHRP then
                                pcall(function()
                                    bHRP.CFrame = blobAnchorPos
                                    bHRP.AssemblyLinearVelocity = Vector3.zero
                                    bHRP.AssemblyAngularVelocity = Vector3.zero
                                end)
                            end
                            task.wait(0.05)
                            local startTime = tick()
                            repeat
                                if CG and weld then pcall(function() CG:FireServer(nil, ttRoot, weld) end) end
                                if CD and weld then pcall(function() CD:FireServer(weld) end) end
                                -- Keep blob anchored each frame
                                if bHRP then
                                    pcall(function()
                                        bHRP.CFrame = blobAnchorPos
                                        bHRP.AssemblyLinearVelocity = Vector3.zero
                                    end)
                                end
                                task.wait()
                            until not _G.LoopKillBlobActive or (tick() - startTime > 2)
                            pcall(function() tt.Character.Humanoid:ChangeState("Dead") end)
                        end
                    end
                end)
                if not _G.LoopKillBlobActive then break end
                task.wait(0.1)
            end
        end)
    end
end
```

})

– –– Ragdoll Target ––
TargetGroup:CreateToggle({
Name     = “Ragdoll Target”,
Flag     = “Ragdoll Target”,
Default  = false,
Callback = function(Value)
SetToggleState(“Ragdoll Target”, Value)
local RS2          = game:GetService(“ReplicatedStorage”)
local DestroyToy   = RS2:WaitForChild(“MenuToys”):WaitForChild(“DestroyToy”)
local SetNetOwner  = RS2:WaitForChild(“GrabEvents”):WaitForChild(“SetNetworkOwner”)
local DestroyLine  = RS2:WaitForChild(“GrabEvents”):WaitForChild(“DestroyGrabLine”)
local toysFolder   = workspace:WaitForChild(Player.Name .. “SpawnedInToys”)
local lpName       = Player.Name

```
    if Value then
        if not selectedKickPlayer then notify("Error", "Select target first", 3); return end

        getgenv().palletRagdollActive = true
        getgenv().PalletForRagdoll    = nil
        getgenv().palletCacheConn     = nil

        local function startPalletCache()
            if getgenv().palletCacheConn then getgenv().palletCacheConn:Disconnect() end
            getgenv().palletCacheConn = toysFolder.ChildAdded:Connect(function(child)
                if not getgenv().palletRagdollActive then return end
                if child.Name ~= "PalletLightBrown" and child.Name ~= "PalletForRagdoll" then return end
                local soundPart = child:WaitForChild("SoundPart", 3)
                if not soundPart then return end
                pcall(function() SetNetOwner:FireServer(soundPart, soundPart.CFrame); DestroyLine:FireServer(soundPart) end)
                local partOwner = soundPart:WaitForChild("PartOwner", 1)
                if partOwner and partOwner.Value == lpName then
                    for _, v in pairs(child:GetChildren()) do
                        if v:IsA("BasePart") then v.CanCollide = false; v.CanQuery = false; v.Transparency = 0.6 end
                    end
                    child.Name = "PalletForRagdoll"
                    getgenv().PalletForRagdoll = child
                    task.spawn(function()
                        while getgenv().palletRagdollActive and child.Parent do
                            local tChar = selectedKickPlayer and selectedKickPlayer.Character
                            local tRoot = tChar and tChar:FindFirstChild("HumanoidRootPart")
                            if tRoot and soundPart.Parent then
                                soundPart.CFrame = CFrame.new(tRoot.Position.X, tRoot.Position.Y + 23, tRoot.Position.Z)
                                task.wait(0.02)
                                soundPart.CFrame = CFrame.new(tRoot.Position.X, tRoot.Position.Y + 19, tRoot.Position.Z)
                                soundPart.AssemblyLinearVelocity = Vector3.new(0, -200, 0)
                            end
                            task.wait(0.08)
                        end
                    end)
                    child.AncestryChanged:Connect(function()
                        if not child.Parent then
                            getgenv().PalletForRagdoll = nil
                            if getgenv().palletRagdollActive then
                                task.wait(0.1)
                                if getgenv().spawnNewPallet then getgenv().spawnNewPallet() end
                            end
                        end
                    end)
                else
                    pcall(function() DestroyToy:FireServer(child) end)
                end
            end)
        end

        getgenv().spawnNewPallet = function()
            if not getgenv().palletRagdollActive then return end
            if getgenv().PalletForRagdoll and getgenv().PalletForRagdoll.Parent then return end
            local c = Player.Character
            local h = c and c:FindFirstChild("HumanoidRootPart")
            if not h then return end
            task.spawn(function()
                pcall(function()
                    RS2.MenuToys.SpawnToyRemoteFunction:InvokeServer("PalletLightBrown", h.CFrame * CFrame.new(0, 10, 20), Vector3.new(0,0,0))
                end)
            end)
        end
        startPalletCache()
        getgenv().spawnNewPallet()
    else
        getgenv().palletRagdollActive = false
        if getgenv().palletCacheConn then getgenv().palletCacheConn:Disconnect(); getgenv().palletCacheConn = nil end
        getgenv().PalletForRagdoll = nil
        if getgenv().spawnNewPallet then getgenv().spawnNewPallet = nil end
        local pf = toysFolder:FindFirstChild("PalletForRagdoll") or toysFolder:FindFirstChild("PalletLightBrown")
        if pf then pcall(function() DestroyToy:FireServer(pf) end) end
    end
end
```

})

– –– Loop Grab Kick (BEST) ––
local LGK_OAT_kickLoopEnabled = false
local LGK_OAT_sno = function(part)
if not part or not part.Parent then return end
pcall(function() RS.GrabEvents.SetNetworkOwner:FireServer(part, part.CFrame) end)
end

TargetGroup:CreateToggle({
Name     = “Loop Grab Kick (BEST)”,
Flag     = “Loop Grab Kick (BEST)”,
Default  = false,
Callback = function(on)
SetToggleState(“Loop Grab Kick (BEST)”, on)
LGK_OAT_kickLoopEnabled = on
if not on then return end
task.spawn(function()
local target = selectedKickPlayer
if not target or not target.Parent then
LGK_OAT_kickLoopEnabled = false
notify(“Error”, “No target selected.”, 3)
return
end
local myChar = Player.Character
local myHRP  = myChar and myChar:FindFirstChild(“HumanoidRootPart”)
local myHead = myChar and myChar:FindFirstChild(“Head”)
if not (myChar and myHRP and myHead) then LGK_OAT_kickLoopEnabled = false; return end
local savedPos = myHRP.CFrame

```
        while LGK_OAT_kickLoopEnabled and R.Heartbeat:Wait() do
            target = selectedKickPlayer
            if not target or not target.Parent then break end
            myChar = Player.Character
            myHRP  = myChar and myChar:FindFirstChild("HumanoidRootPart")
            myHead = myChar and myChar:FindFirstChild("Head")
            if not (myChar and myHRP and myHead) then task.wait(0); continue end

            local tChar = target.Character
            local tHead = tChar and tChar:FindFirstChild("Head")
            local tHum  = tChar and tChar:FindFirstChild("Humanoid")
            if not (tHead and tHum) or tHum.Health == 0 then task.wait(0); continue end

            local BodyPos = tHead:FindFirstChild("BodyPosition")
            if (tHead.Position - myHRP.Position).Magnitude <= 30 then
                if not BodyPos then
                    BodyPos = Instance.new("BodyPosition")
                    BodyPos.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
                    BodyPos.P = 45000; BodyPos.D = 500
                    BodyPos.Parent = tHead
                end
                LGK_OAT_sno(tHead)
                BodyPos.Position = myHead.Position + Vector3.new(0, 17, 0)
                pcall(function() RS.GrabEvents.DestroyGrabLine:FireServer(tHead) end)
            else
                local oldCF = myChar:GetPivot()
                if not tHead or not tHum or tHum.Health == 0 then
                    local newChar = target.CharacterAdded:Wait()
                    tHead = newChar:WaitForChild("Head", 2)
                    tHum  = newChar:FindFirstChild("Humanoid")
                end
                repeat
                    if not LGK_OAT_kickLoopEnabled then break end
                    pcall(function() myChar:PivotTo(tHead.CFrame * CFrame.new(0, 10, 0)) end)
                    LGK_OAT_sno(tHead)
                    task.wait(0)
                until not tHead
                    or (tHead:FindFirstChild("PartOwner") and target:FindFirstChild("IsHeld") and target.IsHeld.Value)
                    or not LGK_OAT_kickLoopEnabled
                if tHead and tHead:FindFirstChild("PartOwner") then
                    task.defer(function()
                        for i = 1, 59 do
                            if not LGK_OAT_kickLoopEnabled or not tHead.Parent then break end
                            pcall(function() tHead.CFrame = oldCF * CFrame.new(0, 15, 0) end)
                            task.wait(0)
                        end
                    end)
                end
                pcall(function() myChar:PivotTo(oldCF) end)
            end
            task.wait(0)
        end

        if target and target.Character then
            local tH = target.Character:FindFirstChild("Head")
            if tH then
                local bp = tH:FindFirstChild("BodyPosition")
                if bp then bp:Destroy() end
                pcall(function() RS.GrabEvents.DestroyGrabLine:FireServer(tH) end)
            end
        end
        LGK_OAT_kickLoopEnabled = false
    end)
end,
```

})

– –– Ragdoll Snowball ––
TargetGroup:CreateToggle({
Name     = “Ragdoll Snowball”,
Flag     = “Ragdoll Snowball”,
Default  = false,
Callback = function(Value)
SetToggleState(“Ragdoll Snowball”, Value)
_G.RagdollSnowballActive = Value
if not Value then return end
local SpawnRemote  = RS:WaitForChild(“MenuToys”):WaitForChild(“SpawnToyRemoteFunction”)
local ToyFolderName = Player.Name .. “SpawnedInToys”
task.spawn(function()
while _G.RagdollSnowballActive do
local target = selectedKickPlayer
if target and target.Character then
local tChar = target.Character
local torso = tChar:FindFirstChild(“HumanoidRootPart”) or tChar:FindFirstChild(“UpperTorso”) or tChar:FindFirstChild(“Torso”)
if torso then
pcall(function()
local offset = Vector3.new(math.random(-5,5)/10, math.random(-5,5)/10, math.random(-5,5)/10)
task.spawn(function() SpawnRemote:InvokeServer(“BallSnowball”, torso.CFrame * CFrame.new(offset), Vector3.zero) end)
end)
local folder = workspace:FindFirstChild(ToyFolderName)
if folder then
for _, snowball in ipairs(folder:GetChildren()) do
if snowball.Name == “BallSnowball” then
local part = snowball:IsA(“BasePart”) and snowball or snowball.PrimaryPart or snowball:FindFirstChildWhichIsA(“BasePart”)
if part then part.CFrame = torso.CFrame; part.AssemblyLinearVelocity = Vector3.zero end
end
end
end
end
end
R.Heartbeat:Wait()
end
end)
end
})

– –– Fling ––
local playerFlingActive = false
local flingBAV          = nil
local originalPos       = nil
TargetGroup:CreateToggle({
Name     = “Fling”,
Flag     = “Fling”,
Default  = false,
Callback = function(on)
SetToggleState(“Fling”, on)
playerFlingActive = on
if on then
if not selectedKickPlayer then notify(“Error”, “Select target first!”, 3); return end
local MyChar = Player.Character
local MyRoot = MyChar and MyChar:FindFirstChild(“HumanoidRootPart”)
if MyRoot then originalPos = MyRoot.CFrame end
notify(“Silenced Hub”, “Fling Mode Activated.”, 3)
task.spawn(function()
while playerFlingActive do
local target = selectedKickPlayer
local char   = Player.Character
local hrp    = char and char:FindFirstChild(“HumanoidRootPart”)
local hum    = char and char:FindFirstChild(“Humanoid”)
if not hrp or not hum then task.wait(0.5); continue end
if target and target.Parent then
local tChar = target.Character
local tRoot = tChar and tChar:FindFirstChild(“HumanoidRootPart”)
local tHum  = tChar and tChar:FindFirstChild(“Humanoid”)
if tRoot and tHum and tHum.Health > 0 then
if not flingBAV or flingBAV.Parent ~= hrp then
if flingBAV then flingBAV:Destroy() end
flingBAV = Instance.new(“BodyAngularVelocity”)
flingBAV.Name = “SilencedSpin”
flingBAV.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
flingBAV.AngularVelocity = Vector3.new(0, 10000, 0)
flingBAV.P = 10000
flingBAV.Parent = hrp
end
for _, part in pairs(char:GetDescendants()) do
if part:IsA(“BasePart”) then part.CanCollide = false end
end
local loop = R.Heartbeat:Connect(function()
if not playerFlingActive or not tRoot or not tRoot.Parent then return end
hrp.CFrame = tRoot.CFrame; hrp.Velocity = Vector3.zero
end)
local startTime = tick()
while tick() - startTime < 1.5 do
if not playerFlingActive or not tRoot.Parent then break end
task.wait(0.1)
end
loop:Disconnect()
else
task.wait(0.2)
end
else
playerFlingActive = false
end
task.wait(0.1)
end
if flingBAV then flingBAV:Destroy(); flingBAV = nil end
local char = Player.Character
if char then
for _, part in pairs(char:GetDescendants()) do
if part:IsA(“BasePart”) then part.CanCollide = true end
end
local hrp = char:FindFirstChild(“HumanoidRootPart”)
if hrp then
hrp.RotVelocity = Vector3.zero; hrp.Velocity = Vector3.zero
if originalPos then hrp.CFrame = originalPos end
end
end
end)
else
playerFlingActive = false
if flingBAV then flingBAV:Destroy(); flingBAV = nil end
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if hrp then hrp.RotVelocity = Vector3.zero; hrp.Velocity = Vector3.zero end
end
end
})

– –– Destroy Gucci (sit) ––
local DestroyTargetGucciActive = false
TargetGroup:CreateToggle({
Name     = “Destroy Gucci (sit)”,
Flag     = “Destroy Gucci (sit)”,
Default  = false,
Callback = function(Value)
SetToggleState(“Destroy Gucci (sit)”, Value)
DestroyTargetGucciActive = Value
if Value then
if not selectedKickPlayer then notify(“Error”, “Select target first”, 3); return end
local char   = Player.Character
local root   = char and char:FindFirstChild(“HumanoidRootPart”)
if not root then return end
local SafeSpot    = root.CFrame
local folderName  = selectedKickPlayer.Name .. “SpawnedInToys”
task.spawn(function()
while DestroyTargetGucciActive do
if not selectedKickPlayer or not selectedKickPlayer.Parent then
DestroyTargetGucciActive = false; break
end
local toysFolder = workspace:FindFirstChild(folderName)
if not toysFolder then task.wait(1); continue end
for _, obj in ipairs(toysFolder:GetChildren()) do
if not DestroyTargetGucciActive then break end
if obj.Name == “CreatureBlobman” or obj.Name == “TractorGreen” then
local seat = obj:FindFirstChild(“VehicleSeat”) or obj:FindFirstChildWhichIsA(“VehicleSeat”, true)
if seat then
local myChar = Player.Character
local myRoot = myChar and myChar:FindFirstChild(“HumanoidRootPart”)
local myHum  = myChar and myChar:FindFirstChild(“Humanoid”)
if myRoot and myHum and myHum.SeatPart ~= seat then
local magnetConn
magnetConn = R.Stepped:Connect(function()
if myRoot and seat then
myRoot.CFrame = seat.CFrame
myRoot.Velocity = Vector3.zero
if obj.PrimaryPart then
obj.PrimaryPart.Velocity = Vector3.zero
obj.PrimaryPart.RotVelocity = Vector3.zero
end
end
end)
local t = tick()
repeat seat:Sit(myHum); R.Heartbeat:Wait() until myHum.SeatPart == seat or tick()-t > 3
magnetConn:Disconnect()
if myHum.SeatPart == seat then
task.wait(0.3)
local bScript = obj:FindFirstChild(“BlobmanSeatAndOwnerScript”)
local CG = bScript and bScript:FindFirstChild(“CreatureGrab”)
local CD = bScript and (bScript:FindFirstChild(“CreatureRelease”) or bScript:FindFirstChild(“CreatureDrop”))
if CG and CD then
for i = 1, 10 do
local rWeld = obj.RightDetector and (obj.RightDetector:FindFirstChild(“RightWeld”) or obj.RightDetector:FindFirstChild(“RigidConstraint”))
if rWeld then
pcall(function() CG:FireServer(obj.RightDetector, obj:FindFirstChild(“HumanoidRootPart”), rWeld) end)
pcall(function() CD:FireServer(rWeld) end)
end
task.wait(0.05)
end
end
myRoot.CFrame = SafeSpot
end
end
end
end
end
task.wait(0.5)
end
end)
end
end
})

– ============================================================
–  METHOD SETTINGS  (Target Tab)
– ============================================================
local MethodGroup = Tabs.Target:CreateBlock({Name = “Silenced”, Side = “Left”})

– State
local methodSettings = {
method      = “Kick”,
delayMode   = “Slower, but better”,
shouldRagdoll     = true,
removeAntiKick    = false,
xOffset     = 0,
yOffset     = 25,
zOffset     = -3,
}

– Helper: check if a target player has an anti-kick shuriken attached
local function removeTargetAntiKick(target)
if not target or not target.Character then return end
local inv = workspace:FindFirstChild(target.Name .. “SpawnedInToys”)
if not inv then return end
local destroyRem = RS:FindFirstChild(“MenuToys”) and RS.MenuToys:FindFirstChild(“DestroyToy”)
if not destroyRem then return end
for _, v in pairs(inv:GetChildren()) do
if v.Name == “NinjaShuriken” or v.Name == “AntiKick” then
pcall(function() destroyRem:FireServer(v) end)
end
end
end

– Helper: ragdoll a target using RagdollRemote
local function ragdollTarget(target)
if not target or not target.Character then return end
local tRoot = target.Character:FindFirstChild(“HumanoidRootPart”)
if not tRoot then return end
pcall(function()
RS.CharacterEvents.RagdollRemote:FireServer(tRoot, 2)
end)
end

– Helper: apply delay based on delayMode
local function methodDelay()
if methodSettings.delayMode == “Slower, but better” then
task.wait(0.08)
elseif methodSettings.delayMode == “Fast” then
task.wait(0.01)
else
task.wait(0)
end
end

– Helper: get the offset CFrame from method settings
local function getMethodOffset()
return CFrame.new(methodSettings.xOffset, methodSettings.yOffset, methodSettings.zOffset)
end

– Apply Method button (runs the selected method on the selected target once)
local methodLoopActive = false

local function runMethod(target)
if not target or not target.Character then
notify(“Method”, “No valid target!”, 3); return
end
local tChar = target.Character
local tRoot = tChar:FindFirstChild(“HumanoidRootPart”)
local tHum  = tChar:FindFirstChildOfClass(“Humanoid”)
if not tRoot or not tHum then return end

```
if methodSettings.removeAntiKick then
    removeTargetAntiKick(target)
end
if methodSettings.shouldRagdoll then
    ragdollTarget(target)
end

local offsetCF = getMethodOffset()
local myChar   = Player.Character
local myRoot   = myChar and myChar:FindFirstChild("HumanoidRootPart")

if methodSettings.method == "Kick" then
    -- Standard blob grab kick
    local char = Player.Character
    local seat = char and char.Humanoid and char.Humanoid.SeatPart
    if not seat or not seat.Parent or seat.Parent.Name ~= "CreatureBlobman" then
        notify("Method", "Not on a blob! Sit in a blob first.", 3); return
    end
    local blobman      = seat.Parent
    local blobHRPM     = blobman:FindFirstChild("HumanoidRootPart") or blobman.PrimaryPart
    local blobAnchorM  = blobHRPM and blobHRPM.CFrame
    -- Keep blob locked in place
    if blobHRPM and blobAnchorM then
        pcall(function()
            blobHRPM.CFrame = blobAnchorM
            blobHRPM.AssemblyLinearVelocity = Vector3.zero
            blobHRPM.AssemblyAngularVelocity = Vector3.zero
        end)
    end
    local remoteFolder = blobman:FindFirstChild("BlobmanSeatAndOwnerScript")
    local grab         = remoteFolder and remoteFolder:FindFirstChild("CreatureGrab")
    local drop         = remoteFolder and remoteFolder:FindFirstChild("CreatureDrop")
    local L_Det  = blobman:FindFirstChild("LeftDetector")
    local R_Det  = blobman:FindFirstChild("RightDetector")
    local L_Weld = L_Det and (L_Det:FindFirstChild("LeftWeld") or L_Det:FindFirstChild("RigidConstraint"))
    local R_Weld = R_Det and (R_Det:FindFirstChild("RightWeld") or R_Det:FindFirstChild("RigidConstraint"))
    if grab and drop then
        -- Bring target to blob's location + offset, not blob to target
        local targetPos = (blobAnchorM or (myRoot and myRoot.CFrame)) * offsetCF
        pcall(function()
            RS.GrabEvents.SetNetworkOwner:FireServer(tRoot, tRoot.CFrame)
            tRoot.CFrame = targetPos
            tRoot.AssemblyLinearVelocity = Vector3.zero
        end)
        pcall(function()
            if blobUseLeftHand  and L_Det and L_Weld then grab:FireServer(L_Det, tRoot, L_Weld); drop:FireServer(L_Weld, tRoot) end
            if blobUseRightHand and R_Det and R_Weld then grab:FireServer(R_Det, tRoot, R_Weld); drop:FireServer(R_Weld, tRoot) end
        end)
    end

elseif methodSettings.method == "Remove Gucci" then
    local folderName  = target.Name .. "SpawnedInToys"
    local toysFolder  = workspace:FindFirstChild(folderName)
    if toysFolder then
        local destroyRem = RS:FindFirstChild("MenuToys") and RS.MenuToys:FindFirstChild("DestroyToy")
        for _, obj in ipairs(toysFolder:GetChildren()) do
            if obj.Name == "CreatureBlobman" or obj.Name == "TractorGreen" then
                if destroyRem then pcall(function() destroyRem:FireServer(obj) end) end
            end
        end
    end

elseif methodSettings.method == "Banana Ragdoll" then
    -- Spawn a banana ball near target to ragdoll them
    local SpawnRemote = RS:WaitForChild("MenuToys"):WaitForChild("SpawnToyRemoteFunction")
    local deliveryCF  = tRoot.CFrame * offsetCF
    pcall(function() SpawnRemote:InvokeServer("BallBanana", deliveryCF, Vector3.new(0, -80, 0)) end)
    task.wait(0.2)
    -- Move the spawned ball onto target
    local folder = workspace:FindFirstChild(Player.Name .. "SpawnedInToys")
    if folder then
        local ball = folder:FindFirstChild("BallBanana")
        if ball then
            local part = ball:IsA("BasePart") and ball or ball.PrimaryPart or ball:FindFirstChildWhichIsA("BasePart")
            if part then
                part.CFrame = tRoot.CFrame
                part.AssemblyLinearVelocity = Vector3.new(0, 50, 0)
            end
        end
    end
end
```

end

MethodGroup:CreateDropdown({
Name    = “Methods”,
List    = {“Kick”, “Remove Gucci”, “Banana Ragdoll”},
Default = “Kick”,
Callback = function(Value)
methodSettings.method = Value
end,
})

MethodGroup:CreateDropdown({
Name    = “Delay Mode”,
List    = {“Slower, but better”, “Fast”, “No Delay”},
Default = “Slower, but better”,
Callback = function(Value)
methodSettings.delayMode = Value
end,
})

MethodGroup:CreateToggle({
Name     = “Should Ragdoll”,
Flag     = “Method Should Ragdoll”,
Default  = true,
Callback = function(Value)
SetToggleState(“Method Should Ragdoll”, Value)
methodSettings.shouldRagdoll = Value
end,
})

MethodGroup:CreateToggle({
Name     = “Remove Anti-Kick [Sticky]”,
Flag     = “Method Remove Anti Kick”,
Default  = false,
Callback = function(Value)
SetToggleState(“Method Remove Anti Kick”, Value)
methodSettings.removeAntiKick = Value
end,
})

MethodGroup:CreateSlider({
Name    = “X Offset”,
Flag    = “Method X Offset”,
Min     = -50,
Max     = 50,
Default = 0,
Rounding = 1,
Callback = function(Value)
methodSettings.xOffset = Value
end,
})

MethodGroup:CreateSlider({
Name    = “Y Offset”,
Flag    = “Method Y Offset”,
Min     = -10,
Max     = 100,
Default = 25,
Rounding = 1,
Callback = function(Value)
methodSettings.yOffset = Value
end,
})

MethodGroup:CreateSlider({
Name    = “Z Offset”,
Flag    = “Method Z Offset”,
Min     = -50,
Max     = 50,
Default = -3,
Rounding = 1,
Callback = function(Value)
methodSettings.zOffset = Value
end,
})

– Apply Method Selected (run once)
MethodGroup:CreateButton({
Name     = “Apply Method Selected”,
Flag     = “Apply Method Selected”,
Callback = function()
runMethod(selectedKickPlayer)
end,
})

– Loop Apply Method Selected (continuous loop)
local loopMethodActive = false
MethodGroup:CreateToggle({
Name     = “Loop Apply Method Selected”,
Flag     = “Loop Apply Method Selected”,
Default  = false,
Callback = function(Value)
SetToggleState(“Loop Apply Method Selected”, Value)
loopMethodActive = Value
if Value then
if not selectedKickPlayer then notify(“Method”, “Select a target first!”, 3); return end
task.spawn(function()
while loopMethodActive do
runMethod(selectedKickPlayer)
methodDelay()
end
end)
end
end,
})

– TP To Void Platform — teleport selected target to a void platform
MethodGroup:CreateButton({
Name     = “TP To Void Platform”,
Flag     = “TP To Void Platform”,
Callback = function()
local target = selectedKickPlayer
if not target or not target.Character then notify(“Method”, “Select a target first!”, 3); return end
local tRoot = target.Character:FindFirstChild(“HumanoidRootPart”)
if not tRoot then return end
– Find a void platform or the lowest platform in workspace
local voidPos = Vector3.new(0, -500, 0)
– Try to find a part named VoidPlatform or FallenPlatform
local platform = workspace:FindFirstChild(“VoidPlatform”) or workspace.Map and workspace.Map:FindFirstChild(“VoidPlatform”)
if platform and platform:IsA(“BasePart”) then
voidPos = platform.Position + Vector3.new(0, 5, 0)
end
pcall(function()
RS.GrabEvents.SetNetworkOwner:FireServer(tRoot, tRoot.CFrame)
tRoot.CFrame = CFrame.new(voidPos)
end)
notify(“Method”, “Teleported target to void area!”, 3)
end,
})

– –– Whitelist ––
WhitelistGroup:CreateDropdown({
Items   = getPlayerList(),
Default = {},
Multi   = true,
Name    = “Whitelist Players”,
})
WhitelistGroup:CreateButton({
Name     = “Refresh List”,
Flag     = “Refresh List”,
Callback = function() updateDropdown() end
})
local notifyActive      = false
local notifyConnection  = nil
WhitelistGroup:CreateToggle({
Name     = “Target Joined Notify”,
Flag     = “Target Joined Notify”,
Default  = false,
Callback = function(on)
SetToggleState(“Target Joined Notify”, on)
notifyActive = on
if on then
notify(“Radar”, “Tracking enabled…”, 3)
if notifyConnection then notifyConnection:Disconnect() end
notifyConnection = PS.PlayerAdded:Connect(function(newPlayer)
if not notifyActive then return end
local detected = false
local reason   = “”
if selectedKickPlayer and newPlayer.Name == selectedKickPlayer.Name then
detected = true; reason = “[Main Target]”
end
if detected then
notify(“Detected”, reason .. “ detected: “ .. newPlayer.Name, 8)
local sound = Instance.new(“Sound”, workspace)
sound.SoundId = “rbxassetid://4590662766”
sound.Volume  = 2
sound:Play()
game:GetService(“Debris”):AddItem(sound, 3)
end
end)
else
if notifyConnection then notifyConnection:Disconnect(); notifyConnection = nil end
notify(“Radar”, “Tracking Disabled”, 2)
end
end
})

– ============================================================
–  GRAB TAB  (GrabGroup reference kept for MassLessGrab)
– ============================================================
local GrabGroup = Tabs.Grab:CreateBlock({Name = “Grab Settings”, Side = “Left”})

GrabGroup:CreateToggle({
Name     = “MassLess Grab”,
Flag     = “MassLess Grab”,
Default  = false,
Callback = function(Value)
SetToggleState(“MassLess Grab”, Value)
_G.MassLessGrab = Value
if not _G.MassLessGrab then
if _G.MLConn then _G.MLConn:Disconnect(); _G.MLConn = nil end
return
end
if _G.MLConn then _G.MLConn:Disconnect(); _G.MLConn = nil end
_G.MLSense = _G.MLSense or 200
_G.MLConn = game:GetService(“RunService”).Heartbeat:Connect(function()
if not _G.MassLessGrab then return end
local gp = workspace:FindFirstChild(“GrabParts”)
if not gp then return end
local dp = gp:FindFirstChild(“DragPart”)
if not dp then return end
local ap = dp:FindFirstChild(“AlignPosition”)
local ao = dp:FindFirstChild(“AlignOrientation”)
if ap then ap.Responsiveness = _G.MLSense; ap.MaxForce = math.huge; ap.MaxVelocity = math.huge end
if ao then ao.Responsiveness = _G.MLSense; ao.MaxTorque = math.huge end
end)
end
})

– ============================================================
–  FUN TAB — ANIMATIONS & TROLL
– ============================================================
local AnimationsGroup = Tabs.Fun:CreateBlock({Name = “Animations”, Side = “Left”})
local FanGroup        = Tabs.Fun:CreateBlock({Name = “Troll”,      Side = “Left”})

– Fake Death
AnimationsGroup:CreateToggle({
Name     = “Fake Death”,
Flag     = “Fake Death”,
Default  = false,
Callback = function(on)
SetToggleState(“Fake Death”, on)
local char = LocalPlayer.Character
if not char then return end
local hum = char:FindFirstChildOfClass(“Humanoid”)
if not hum then return end
if on then hum:ChangeState(Enum.HumanoidStateType.Physics); hum.PlatformStand = true
else hum.PlatformStand = false; hum:ChangeState(Enum.HumanoidStateType.GettingUp) end
end
})

– Fake Lag
local fakeLagConn
AnimationsGroup:CreateToggle({
Name     = “Fake Lag”,
Flag     = “Fake Lag”,
Default  = false,
Callback = function(on)
SetToggleState(“Fake Lag”, on)
if fakeLagConn then fakeLagConn:Disconnect(); fakeLagConn = nil end
if not on then return end
fakeLagConn = RunService.Heartbeat:Connect(function()
local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild(“HumanoidRootPart”)
if not root then return end
if math.random(1,5) == 1 then
root.CFrame = root.CFrame * CFrame.new(math.random(-2,2)/10, 0, math.random(-2,2)/10)
end
end)
end
})

– Bang (Slow)
local playBangActive = false
local bangAnimTrack  = nil
local bangAnimId     = “rbxassetid://148840371”
local bangSpeed      = 10

local function startBang()
local plrB = Players.LocalPlayer
local char = plrB.Character or plrB.CharacterAdded:Wait()
local hum  = char:FindFirstChildOfClass(“Humanoid”)
if not hum then return end
local animator = hum:FindFirstChildOfClass(“Animator”)
if not animator then animator = Instance.new(“Animator”); animator.Parent = hum end
local anim = Instance.new(“Animation”)
anim.AnimationId = bangAnimId
bangAnimTrack = animator:LoadAnimation(anim)
bangAnimTrack.Priority = Enum.AnimationPriority.Action
bangAnimTrack:Play()
bangAnimTrack:AdjustSpeed(bangSpeed)
task.spawn(function()
while playBangActive do
task.wait(0.1)
if bangAnimTrack and bangAnimTrack.IsPlaying then bangAnimTrack.TimePosition = 0.1 end
end
end)
end

local function stopBang()
if bangAnimTrack then bangAnimTrack:Stop(); bangAnimTrack = nil end
end

FanGroup:CreateToggle({
Name     = “Bang (Slow)”,
Flag     = “Bang (Slow)”,
Default  = false,
Callback = function(on)
SetToggleState(“Bang (Slow)”, on)
playBangActive = on
if on then startBang() else stopBang() end
end
})

– UFO Shuriken Stick
FanGroup:CreateToggle({
Name     = “Stick Shuriken to UFO”,
Flag     = “Stick Shuriken to UFO”,
Default  = false,
Callback = function(state)
SetToggleState(“Stick Shuriken to UFO”, state)
if not state then return end
local plrUFO = game:GetService(“Players”).LocalPlayer
local RSUFO  = game:GetService(“ReplicatedStorage”)
local PlayerNameUFO = plrUFO.Name
local StickyEvent   = RSUFO:WaitForChild(“PlayerEvents”):WaitForChild(“StickyPartEvent”)
local SpawnRemote   = RSUFO.MenuToys:WaitForChild(“SpawnToyRemoteFunction”)
local CanSpawn      = plrUFO:WaitForChild(“CanSpawnToy”)
local ToysFolder    = workspace:WaitForChild(PlayerNameUFO .. “SpawnedInToys”)
local UFOs = {
workspace.Map.AlwaysHereTweenedObjects:FindFirstChild(“InnerUFO”),
workspace.Map.AlwaysHereTweenedObjects:FindFirstChild(“OuterUFO”)
}
local function getHRPUFO()
if plrUFO.Character and plrUFO.Character:FindFirstChild(“HumanoidRootPart”) then
return plrUFO.Character.HumanoidRootPart
end
return plrUFO.CharacterAdded:Wait():WaitForChild(“HumanoidRootPart”)
end
task.spawn(function()
for i = 1, 12 do
local t = tick()
while not CanSpawn.Value do
if tick()-t>5 then break end
task.wait(0.1)
end
local hrpUFO = getHRPUFO()
if hrpUFO then
pcall(function() SpawnRemote:InvokeServer(“NinjaShuriken”, hrpUFO.CFrame * CFrame.new(0,10,15), Vector3.new()) end)
end
task.wait(0.15)
end
task.wait(1)
for _, Toy in ipairs(ToysFolder:GetChildren()) do
if Toy.Name == “NinjaShuriken” and Toy:FindFirstChild(“StickyPart”) then
for _, UFO in ipairs(UFOs) do
if UFO and UFO:FindFirstChild(“Object”) and UFO.Object:FindFirstChild(“ObjectModel”) and UFO.Object.ObjectModel:FindFirstChild(“Body”) then
StickyEvent:FireServer(Toy.StickyPart, UFO.Object.ObjectModel.Body, CFrame.new())
local follow = UFO.Object:FindFirstChild(“FollowThisPart”)
if follow then
if follow:FindFirstChild(“AlignOrientation”) then follow.AlignOrientation.Enabled = false end
if follow:FindFirstChild(“AlignPosition”)    then follow.AlignPosition.Enabled = false end
end
end
end
end
end
end)
end
})

– ============================================================
–  GRAB TAB — EXTENDED
– ============================================================
local GrabExtGroup   = Tabs.Grab:CreateBlock({Name = “Grab Tools”,        Side = “Left”})
local GrabTrollGroup = Tabs.Grab:CreateBlock({Name = “Grab Tricks”,       Side = “Right”})
local GrabThrowGroup = Tabs.Grab:CreateBlock({Name = “Throw & Fling”,     Side = “Right”})

– Grab Sense slider
GrabGroup:CreateSlider({
Name     = “MassLess Grab Sense”,
Flag     = “MassLess Grab Sense”,
Min      = 10,
Max      = 1000,
Default  = 200,
Rounding = 1,
Callback = function(v)
_G.MLSense = v
end,
})

– Long Grab — extends your grab reach by boosting AlignPosition MaxVelocity
local longGrabConn
GrabGroup:CreateToggle({
Name     = “Long Grab (Reach Extend)”,
Flag     = “Long Grab”,
Default  = false,
Callback = function(v)
SetToggleState(“Long Grab”, v)
if longGrabConn then longGrabConn:Disconnect(); longGrabConn = nil end
if not v then return end
longGrabConn = R.Heartbeat:Connect(function()
local gp = workspace:FindFirstChild(“GrabParts”)
if not gp then return end
local dp = gp:FindFirstChild(“DragPart”)
if not dp then return end
local ap = dp:FindFirstChild(“AlignPosition”)
if ap then
ap.MaxForce    = math.huge
ap.MaxVelocity = math.huge
ap.Responsiveness = 200
end
end)
end,
})

– No Grab Struggle — continuously fires struggle event so grabbed players can’t hold you
local noStruggleConn
GrabExtGroup:CreateToggle({
Name     = “No Grab Struggle (spam struggle)”,
Flag     = “No Grab Struggle”,
Default  = false,
Callback = function(v)
SetToggleState(“No Grab Struggle”, v)
if noStruggleConn then noStruggleConn:Disconnect(); noStruggleConn = nil end
if not v then return end
noStruggleConn = R.Heartbeat:Connect(function()
if not Player.Character then return end
local isHeld = Player:FindFirstChild(“IsHeld”)
if isHeld and isHeld.Value then
pcall(function() RS.CharacterEvents.Struggle:FireServer() end)
end
end)
end,
})

– Auto Struggle — always spam struggle whenever IsHeld
local autoStruggleConn
GrabExtGroup:CreateToggle({
Name     = “Auto Struggle (always)”,
Flag     = “Auto Struggle”,
Default  = false,
Callback = function(v)
SetToggleState(“Auto Struggle”, v)
if autoStruggleConn then autoStruggleConn:Disconnect(); autoStruggleConn = nil end
if not v then return end
autoStruggleConn = R.Heartbeat:Connect(function()
pcall(function() RS.CharacterEvents.Struggle:FireServer() end)
end)
end,
})

– Grab Freeze — anchor your HRP the moment someone grabs you
local grabFreezeConn
GrabExtGroup:CreateToggle({
Name     = “Grab Freeze (anchor when held)”,
Flag     = “Grab Freeze”,
Default  = false,
Callback = function(v)
SetToggleState(“Grab Freeze”, v)
if grabFreezeConn then grabFreezeConn:Disconnect(); grabFreezeConn = nil end
if not v then return end
local isHeld = Player:WaitForChild(“IsHeld”, 5)
if not isHeld then return end
grabFreezeConn = isHeld:GetPropertyChangedSignal(“Value”):Connect(function()
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if not hrp then return end
if isHeld.Value then
hrp.Anchored = true
hrp.AssemblyLinearVelocity = Vector3.zero
else
hrp.Anchored = false
end
end)
end,
})

– Grab Teleport — TP to whoever is grabbing you
GrabExtGroup:CreateButton({
Name     = “TP To Grabber”,
Flag     = “TP To Grabber”,
Callback = function()
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if not hrp then return end
for _, plr in ipairs(PS:GetPlayers()) do
if plr ~= Player and plr.Character then
local seat = plr.Character:FindFirstChildWhichIsA(“VehicleSeat”, true)
if seat and seat.Occupant and seat.Occupant.Parent == Player.Character then
hrp.CFrame = plr.Character:GetPivot() * CFrame.new(0, 3, 0)
notify(“Grab”, “Teleported to “ .. plr.Name, 3)
return
end
end
end
notify(“Grab”, “No one is grabbing you!”, 3)
end,
})

– Sticky Grab — fire StickyPartEvent on a player near you
GrabExtGroup:CreateButton({
Name     = “Sticky Grab Target”,
Flag     = “Sticky Grab Target”,
Callback = function()
local target = selectedKickPlayer
if not target or not target.Character then notify(“Grab”, “Select a target first!”, 3); return end
local myChar = Player.Character
local myHRP  = myChar and myChar:FindFirstChild(“HumanoidRootPart”)
local tRoot  = target.Character:FindFirstChild(“HumanoidRootPart”)
if not myHRP or not tRoot then return end
local stickyEvent = RS:FindFirstChild(“PlayerEvents”) and RS.PlayerEvents:FindFirstChild(“StickyPartEvent”)
if stickyEvent then
pcall(function() stickyEvent:FireServer(myHRP, tRoot, CFrame.new()) end)
notify(“Grab”, “Sticky fired on “ .. target.Name, 3)
end
end,
})

– Grab Tricks
local spinGrabConn
GrabTrollGroup:CreateToggle({
Name     = “Spin Grabbed Player”,
Flag     = “Spin Grabbed Player”,
Default  = false,
Callback = function(v)
SetToggleState(“Spin Grabbed Player”, v)
if spinGrabConn then spinGrabConn:Disconnect(); spinGrabConn = nil end
if not v then return end
local spinAngle = 0
spinGrabConn = R.Heartbeat:Connect(function()
local gp = workspace:FindFirstChild(“GrabParts”)
local dp = gp and gp:FindFirstChild(“DragPart”)
if not dp then return end
spinAngle = spinAngle + 5
local ap = dp:FindFirstChild(“AlignPosition”)
if ap then
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if hrp then
local radius = 5
local offsetX = math.cos(math.rad(spinAngle)) * radius
local offsetZ = math.sin(math.rad(spinAngle)) * radius
ap.Position = hrp.Position + Vector3.new(offsetX, 4, offsetZ)
end
end
end)
end,
})

– Grab height — offset the grabbed player up/down
local grabHeightVal = 4
GrabTrollGroup:CreateSlider({
Name     = “Grab Hold Height”,
Flag     = “Grab Hold Height”,
Min      = -10,
Max      = 30,
Default  = 4,
Rounding = 1,
Callback = function(v) grabHeightVal = v end,
})

– Freeze grabbed player in place
local freezeGrabbedConn
GrabTrollGroup:CreateToggle({
Name     = “Freeze Grabbed Player”,
Flag     = “Freeze Grabbed Player”,
Default  = false,
Callback = function(v)
SetToggleState(“Freeze Grabbed Player”, v)
if freezeGrabbedConn then freezeGrabbedConn:Disconnect(); freezeGrabbedConn = nil end
if not v then return end
freezeGrabbedConn = R.Heartbeat:Connect(function()
local gp = workspace:FindFirstChild(“GrabParts”)
local dp = gp and gp:FindFirstChild(“DragPart”)
if not dp then return end
local ap = dp:FindFirstChild(“AlignPosition”)
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if ap and hrp then
ap.Position = hrp.Position + Vector3.new(0, grabHeightVal, 0)
end
end)
end,
})

– Throw & Fling
local throwPower = 300
GrabThrowGroup:CreateSlider({
Name     = “Throw Power”,
Flag     = “Throw Power”,
Min      = 50,
Max      = 5000,
Default  = 300,
Rounding = 10,
Callback = function(v) throwPower = v end,
})

GrabThrowGroup:CreateButton({
Name     = “Throw Grabbed Player (Up)”,
Flag     = “Throw Up”,
Callback = function()
local gp = workspace:FindFirstChild(“GrabParts”)
local dp = gp and gp:FindFirstChild(“DragPart”)
if not dp then notify(“Grab”, “Not grabbing anyone!”, 3); return end
local ap = dp:FindFirstChild(“AlignPosition”)
if ap then
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if hrp then
ap.Position = hrp.Position + Vector3.new(0, throwPower / 10, 0)
task.wait(0.05)
end
end
– Also blast the DragPart velocity
pcall(function() dp.AssemblyLinearVelocity = Vector3.new(0, throwPower, 0) end)
notify(“Grab”, “Threw player upward!”, 2)
end,
})

GrabThrowGroup:CreateButton({
Name     = “Throw Grabbed Player (Forward)”,
Flag     = “Throw Forward”,
Callback = function()
local gp = workspace:FindFirstChild(“GrabParts”)
local dp = gp and gp:FindFirstChild(“DragPart”)
if not dp then notify(“Grab”, “Not grabbing anyone!”, 3); return end
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if not hrp then return end
local fwd = hrp.CFrame.LookVector
pcall(function() dp.AssemblyLinearVelocity = fwd * throwPower end)
notify(“Grab”, “Threw player forward!”, 2)
end,
})

GrabThrowGroup:CreateButton({
Name     = “YEET (all directions random)”,
Flag     = “YEET”,
Callback = function()
local gp = workspace:FindFirstChild(“GrabParts”)
local dp = gp and gp:FindFirstChild(“DragPart”)
if not dp then notify(“Grab”, “Not grabbing anyone!”, 3); return end
local rx = math.random(-1, 1)
local rz = math.random(-1, 1)
pcall(function()
dp.AssemblyLinearVelocity = Vector3.new(rx * throwPower, throwPower * 0.6, rz * throwPower)
end)
notify(“Grab”, “YEETED!”, 2)
end,
})

– Loop throw upward
local loopThrowConn
GrabThrowGroup:CreateToggle({
Name     = “Loop Throw (Up)”,
Flag     = “Loop Throw Up”,
Default  = false,
Callback = function(v)
SetToggleState(“Loop Throw Up”, v)
if loopThrowConn then loopThrowConn:Disconnect(); loopThrowConn = nil end
if not v then return end
loopThrowConn = R.Heartbeat:Connect(function()
local gp = workspace:FindFirstChild(“GrabParts”)
local dp = gp and gp:FindFirstChild(“DragPart”)
if not dp then return end
pcall(function() dp.AssemblyLinearVelocity = Vector3.new(0, throwPower, 0) end)
end)
end,
})

– Strength Grab — maximises AlignPosition force AND sets DragPart mass to 0 so it feels weightless
local strengthGrabConn
GrabThrowGroup:CreateToggle({
Name     = “Strength Grab (Massless Object)”,
Flag     = “Strength Grab”,
Default  = false,
Callback = function(v)
SetToggleState(“Strength Grab”, v)
if strengthGrabConn then strengthGrabConn:Disconnect(); strengthGrabConn = nil end
if not v then
– restore DragPart mass if it exists
local gp = workspace:FindFirstChild(“GrabParts”)
local dp = gp and gp:FindFirstChild(“DragPart”)
if dp then pcall(function() dp.CustomPhysicalProperties = PhysicalProperties.new(0.7, 0.3, 0.5, 0.1, 0.1) end) end
return
end
strengthGrabConn = R.Heartbeat:Connect(function()
local gp = workspace:FindFirstChild(“GrabParts”)
local dp = gp and gp:FindFirstChild(“DragPart”)
if not dp then return end
– Make object massless (density 0)
pcall(function()
dp.CustomPhysicalProperties = PhysicalProperties.new(0, 0, 0, 0, 0)
end)
– Max force / velocity so it moves like it weighs nothing
local ap = dp:FindFirstChild(“AlignPosition”)
local ao = dp:FindFirstChild(“AlignOrientation”)
if ap then ap.MaxForce = math.huge; ap.MaxVelocity = math.huge; ap.Responsiveness = 200 end
if ao then ao.MaxTorque = math.huge; ao.Responsiveness = 200 end
end)
notify(“Grab”, “Strength Grab ON — object is massless!”, 3)
end,
})

– Kill Grab — instantly kills any player whose HumanoidRootPart touches the DragPart
local killGrabConn
GrabThrowGroup:CreateToggle({
Name     = “Kill Grab (touch = instant kill)”,
Flag     = “Kill Grab”,
Default  = false,
Callback = function(v)
SetToggleState(“Kill Grab”, v)
if killGrabConn then killGrabConn:Disconnect(); killGrabConn = nil end
if not v then return end
killGrabConn = R.Heartbeat:Connect(function()
local gp = workspace:FindFirstChild(“GrabParts”)
local dp = gp and gp:FindFirstChild(“DragPart”)
if not dp then return end
local dpPos = dp.Position
for _, plr in ipairs(PS:GetPlayers()) do
if plr ~= Player and plr.Character then
local tRoot = plr.Character:FindFirstChild(“HumanoidRootPart”)
local tHum  = plr.Character:FindFirstChildOfClass(“Humanoid”)
if tRoot and tHum and tHum.Health > 0 then
if (tRoot.Position - dpPos).Magnitude <= 5 then
pcall(function() tHum.Health = 0 end)
end
end
end
end
end)
notify(“Grab”, “Kill Grab ON — touch kills!”, 3)
end,
})

– Glitch Launch — flings the grabbed player (DragPart) 35,000 studs into the sky
GrabThrowGroup:CreateButton({
Name     = “Glitch Launch (35k studs up)”,
Flag     = “Glitch Launch”,
Callback = function()
local gp = workspace:FindFirstChild(“GrabParts”)
local dp = gp and gp:FindFirstChild(“DragPart”)
if not dp then notify(“Grab”, “Not grabbing anyone!”, 3); return end
local ap = dp:FindFirstChild(“AlignPosition”)
if ap then
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if hrp then
– Teleport AlignPosition target to 35k studs up
ap.Position = hrp.Position + Vector3.new(0, 35000, 0)
end
end
– Also directly blast the DragPart upward
pcall(function()
dp.AssemblyLinearVelocity = Vector3.new(0, 35000, 0)
end)
notify(“Grab”, “Glitch Launch fired — 35,000 studs up!”, 3)
end,
})

– ============================================================
–  PLAYER TAB
– ============================================================
local PlayerMovGroup  = Tabs.Player:CreateBlock({Name = “Movement”,       Side = “Left”})
local PlayerCharGroup = Tabs.Player:CreateBlock({Name = “Character”,      Side = “Left”})
local PlayerTPGroup   = Tabs.Player:CreateBlock({Name = “Teleport”,       Side = “Right”})
local PlayerVisGroup  = Tabs.Player:CreateBlock({Name = “Visuals”,        Side = “Right”})
local PlayerUtilGroup = Tabs.Player:CreateBlock({Name = “Utilities”,      Side = “Right”})

– Walk Speed
local defaultWalkSpeed = 16
PlayerMovGroup:CreateSlider({
Name     = “Walk Speed”,
Flag     = “Walk Speed”,
Min      = 0,
Max      = 500,
Default  = 16,
Rounding = 1,
Callback = function(v)
local char = Player.Character
local hum  = char and char:FindFirstChildOfClass(“Humanoid”)
if hum then hum.WalkSpeed = v end
defaultWalkSpeed = v
end,
})

– Jump Power
PlayerMovGroup:CreateSlider({
Name     = “Jump Power”,
Flag     = “Jump Power”,
Min      = 0,
Max      = 500,
Default  = 50,
Rounding = 1,
Callback = function(v)
local char = Player.Character
local hum  = char and char:FindFirstChildOfClass(“Humanoid”)
if hum then hum.JumpPower = v end
end,
})

– Infinite Jump
local infJumpConn
PlayerMovGroup:CreateToggle({
Name     = “Infinite Jump”,
Flag     = “Infinite Jump”,
Default  = false,
Callback = function(v)
SetToggleState(“Infinite Jump”, v)
if infJumpConn then infJumpConn:Disconnect(); infJumpConn = nil end
if not v then return end
infJumpConn = UserInputService.JumpRequest:Connect(function()
local char = Player.Character
local hum  = char and char:FindFirstChildOfClass(“Humanoid”)
if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
end)
end,
})

– No Clip
local noClipConn
PlayerMovGroup:CreateToggle({
Name     = “No Clip”,
Flag     = “No Clip”,
Default  = false,
Callback = function(v)
SetToggleState(“No Clip”, v)
if noClipConn then noClipConn:Disconnect(); noClipConn = nil end
if not v then
local char = Player.Character
if char then
for _, p in pairs(char:GetDescendants()) do
if p:IsA(“BasePart”) then p.CanCollide = true end
end
end
return
end
noClipConn = R.Stepped:Connect(function()
local char = Player.Character
if not char then return end
for _, p in pairs(char:GetDescendants()) do
if p:IsA(“BasePart”) then p.CanCollide = false end
end
end)
end,
})

– Anti Gravity (low gravity)
local antiGravConn
PlayerMovGroup:CreateToggle({
Name     = “Low Gravity”,
Flag     = “Low Gravity”,
Default  = false,
Callback = function(v)
SetToggleState(“Low Gravity”, v)
if antiGravConn then antiGravConn:Disconnect(); antiGravConn = nil end
if v then
workspace.Gravity = 20
else
workspace.Gravity = 196.2
end
end,
})

– Gravity slider
PlayerMovGroup:CreateSlider({
Name     = “Gravity”,
Flag     = “Gravity Slider”,
Min      = 0,
Max      = 500,
Default  = 196,
Rounding = 1,
Callback = function(v)
workspace.Gravity = v
end,
})

– Speed Boost toggle (2x walk speed)
PlayerMovGroup:CreateToggle({
Name     = “Speed Boost (2x)”,
Flag     = “Speed Boost”,
Default  = false,
Callback = function(v)
SetToggleState(“Speed Boost”, v)
local char = Player.Character
local hum  = char and char:FindFirstChildOfClass(“Humanoid”)
if hum then hum.WalkSpeed = v and (defaultWalkSpeed * 2) or defaultWalkSpeed end
end,
})

– Fly — basic fly with WASD + Space/Ctrl
local flyActive  = false
local flyConn
local flyBodyVel, flyBodyGyro
PlayerMovGroup:CreateToggle({
Name     = “Fly”,
Flag     = “Fly”,
Default  = false,
Callback = function(v)
SetToggleState(“Fly”, v)
flyActive = v
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
local hum  = char and char:FindFirstChildOfClass(“Humanoid”)
if not hrp or not hum then return end
if v then
hum.PlatformStand = true
flyBodyVel = Instance.new(“BodyVelocity”)
flyBodyVel.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
flyBodyVel.Velocity = Vector3.zero
flyBodyVel.P = 9e4
flyBodyVel.Parent = hrp
flyBodyGyro = Instance.new(“BodyGyro”)
flyBodyGyro.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
flyBodyGyro.D = 100
flyBodyGyro.P = 9e4
flyBodyGyro.Parent = hrp
local flySpeed = 60
flyConn = R.RenderStepped:Connect(function()
if not flyActive or not hrp or not hrp.Parent then return end
local cam     = workspace.CurrentCamera
local moveDir = Vector3.zero
if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + cam.CFrame.LookVector end
if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - cam.CFrame.LookVector end
if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - cam.CFrame.RightVector end
if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + cam.CFrame.RightVector end
if UserInputService:IsKeyDown(Enum.KeyCode.Space) then moveDir = moveDir + Vector3.new(0, 1, 0) end
if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then moveDir = moveDir - Vector3.new(0, 1, 0) end
flyBodyVel.Velocity = moveDir.Magnitude > 0 and (moveDir.Unit * flySpeed) or Vector3.zero
flyBodyGyro.CFrame  = cam.CFrame
end)
else
hum.PlatformStand = false
if flyConn then flyConn:Disconnect(); flyConn = nil end
if flyBodyVel then flyBodyVel:Destroy(); flyBodyVel = nil end
if flyBodyGyro then flyBodyGyro:Destroy(); flyBodyGyro = nil end
end
end,
})

– Fly speed
PlayerMovGroup:CreateSlider({
Name     = “Fly Speed”,
Flag     = “Fly Speed”,
Min      = 10,
Max      = 500,
Default  = 60,
Rounding = 5,
Callback = function(v)
– stored in flyBodyVel if active; toggling fly re-reads
if flyBodyVel then flyBodyVel.Velocity = flyBodyVel.Velocity.Unit * v end
end,
})

– Character settings
PlayerCharGroup:CreateToggle({
Name     = “God Mode (max health loop)”,
Flag     = “God Mode”,
Default  = false,
Callback = function(v)
SetToggleState(“God Mode”, v)
task.spawn(function()
while GetToggleState(“God Mode”) do
local char = Player.Character
local hum  = char and char:FindFirstChildOfClass(“Humanoid”)
if hum then hum.Health = hum.MaxHealth end
task.wait(0.1)
end
end)
end,
})

PlayerCharGroup:CreateToggle({
Name     = “Invisible (local)”,
Flag     = “Invisible Local”,
Default  = false,
Callback = function(v)
SetToggleState(“Invisible Local”, v)
local char = Player.Character
if not char then return end
for _, p in pairs(char:GetDescendants()) do
if p:IsA(“BasePart”) or p:IsA(“Decal”) then
p.LocalTransparencyModifier = v and 1 or 0
end
end
end,
})

PlayerCharGroup:CreateToggle({
Name     = “Force Field”,
Flag     = “Force Field”,
Default  = false,
Callback = function(v)
SetToggleState(“Force Field”, v)
local char = Player.Character
if not char then return end
if v then
local ff = Instance.new(“ForceField”)
ff.Visible = true
ff.Parent = char
else
for _, ff in pairs(char:GetChildren()) do
if ff:IsA(“ForceField”) then ff:Destroy() end
end
end
end,
})

– Character size
PlayerCharGroup:CreateSlider({
Name     = “Character Scale”,
Flag     = “Character Scale”,
Min      = 10,
Max      = 300,
Default  = 100,
Rounding = 5,
Callback = function(v)
local char = Player.Character
local hum  = char and char:FindFirstChildOfClass(“Humanoid”)
if not hum then return end
local scale = v / 100
local desc = hum:FindFirstChildOfClass(“HumanoidDescription”) or Instance.new(“HumanoidDescription”)
desc.HeadScale  = scale
desc.BodyWidthScale = scale
desc.BodyHeightScale = scale
desc.BodyDepthScale  = scale
pcall(function() hum:ApplyDescription(desc) end)
end,
})

– Reset character
PlayerCharGroup:CreateButton({
Name     = “Reset Character”,
Flag     = “Reset Character”,
Callback = function()
local char = Player.Character
local hum  = char and char:FindFirstChildOfClass(“Humanoid”)
if hum then hum.Health = 0 end
end,
})

– Teleport options
PlayerTPGroup:CreateButton({
Name     = “TP to Spawn”,
Flag     = “TP to Spawn”,
Callback = function()
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if not hrp then return end
local spawn = workspace:FindFirstChild(“SpawnLocation”)
if spawn and spawn:IsA(“BasePart”) then
hrp.CFrame = spawn.CFrame + Vector3.new(0, 5, 0)
else
hrp.CFrame = CFrame.new(0, 10, 0)
end
notify(“Player”, “Teleported to spawn!”, 2)
end,
})

PlayerTPGroup:CreateButton({
Name     = “TP to Selected Target”,
Flag     = “TP to Target”,
Callback = function()
local target = selectedKickPlayer
if not target or not target.Character then notify(“Player”, “No target selected!”, 3); return end
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
local tRoot = target.Character:FindFirstChild(“HumanoidRootPart”)
if hrp and tRoot then
hrp.CFrame = tRoot.CFrame * CFrame.new(0, 0, 3)
notify(“Player”, “Teleported to “ .. target.Name, 2)
end
end,
})

PlayerTPGroup:CreateButton({
Name     = “TP Target to Me”,
Flag     = “TP Target to Me”,
Callback = function()
local target = selectedKickPlayer
if not target or not target.Character then notify(“Player”, “No target selected!”, 3); return end
local char  = Player.Character
local myHRP = char and char:FindFirstChild(“HumanoidRootPart”)
local tRoot = target.Character:FindFirstChild(“HumanoidRootPart”)
if myHRP and tRoot then
pcall(function()
RS.GrabEvents.SetNetworkOwner:FireServer(tRoot, tRoot.CFrame)
tRoot.CFrame = myHRP.CFrame * CFrame.new(2, 0, 2)
end)
notify(“Player”, “Brought “ .. target.Name .. “ to you!”, 2)
end
end,
})

– Random TP
PlayerTPGroup:CreateButton({
Name     = “Random TP”,
Flag     = “Random TP”,
Callback = function()
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if hrp then
hrp.CFrame = CFrame.new(math.random(-500, 500), 100, math.random(-500, 500))
notify(“Player”, “Teleported randomly!”, 2)
end
end,
})

– Saved position
local savedPlayerPos = nil
PlayerTPGroup:CreateButton({
Name     = “Save Position”,
Flag     = “Save Position”,
Callback = function()
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if hrp then
savedPlayerPos = hrp.CFrame
notify(“Player”, “Position saved!”, 2)
end
end,
})

PlayerTPGroup:CreateButton({
Name     = “Load Saved Position”,
Flag     = “Load Position”,
Callback = function()
if not savedPlayerPos then notify(“Player”, “No position saved!”, 3); return end
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if hrp then
hrp.CFrame = savedPlayerPos
notify(“Player”, “Position restored!”, 2)
end
end,
})

– Visuals
PlayerVisGroup:CreateToggle({
Name     = “ESP (box highlight all players)”,
Flag     = “Player ESP”,
Default  = false,
Callback = function(v)
SetToggleState(“Player ESP”, v)
– Remove existing highlights
for _, plr in pairs(PS:GetPlayers()) do
if plr ~= Player and plr.Character then
local hl = plr.Character:FindFirstChild(“SilencedESP”)
if hl then hl:Destroy() end
end
end
if not v then return end
local function addESP(char)
if not char then return end
local existing = char:FindFirstChild(“SilencedESP”)
if existing then existing:Destroy() end
local hl = Instance.new(“Highlight”)
hl.Name = “SilencedESP”
hl.FillTransparency = 0.6
hl.OutlineTransparency = 0
hl.FillColor    = Color3.fromRGB(255, 50, 50)
hl.OutlineColor = Color3.fromRGB(255, 255, 0)
hl.Parent = char
end
for _, plr in pairs(PS:GetPlayers()) do
if plr ~= Player then addESP(plr.Character) end
plr.CharacterAdded:Connect(function(c)
if GetToggleState(“Player ESP”) then task.wait(0.5); addESP(c) end
end)
end
PS.PlayerAdded:Connect(function(plr)
plr.CharacterAdded:Connect(function(c)
if GetToggleState(“Player ESP”) then task.wait(0.5); addESP(c) end
end)
end)
end,
})

PlayerVisGroup:CreateToggle({
Name     = “Highlight Self”,
Flag     = “Highlight Self”,
Default  = false,
Callback = function(v)
SetToggleState(“Highlight Self”, v)
local char = Player.Character
if not char then return end
local hl = char:FindFirstChild(“SilencedSelfHL”)
if v then
if not hl then
hl = Instance.new(“Highlight”)
hl.Name = “SilencedSelfHL”
hl.FillColor    = Color3.fromRGB(0, 200, 255)
hl.OutlineColor = Color3.fromRGB(255, 255, 255)
hl.FillTransparency = 0.5
hl.Parent = char
end
else
if hl then hl:Destroy() end
end
end,
})

PlayerVisGroup:CreateToggle({
Name     = “Full Bright”,
Flag     = “Full Bright”,
Default  = false,
Callback = function(v)
SetToggleState(“Full Bright”, v)
local lighting = game:GetService(“Lighting”)
if v then
lighting.Brightness = 2
lighting.ClockTime  = 14
lighting.FogEnd     = 100000
lighting.GlobalShadows = false
lighting.Ambient    = Color3.fromRGB(255, 255, 255)
lighting.OutdoorAmbient = Color3.fromRGB(255, 255, 255)
else
lighting.Brightness = 1
lighting.ClockTime  = 14
lighting.FogEnd     = 100000
lighting.GlobalShadows = true
lighting.Ambient    = Color3.fromRGB(70, 70, 70)
lighting.OutdoorAmbient = Color3.fromRGB(70, 70, 70)
end
end,
})

PlayerVisGroup:CreateToggle({
Name     = “Camera Lock (lock cam to char)”,
Flag     = “Camera Lock”,
Default  = false,
Callback = function(v)
SetToggleState(“Camera Lock”, v)
local cam = workspace.CurrentCamera
if v then
cam.CameraType = Enum.CameraType.Attach
else
cam.CameraType = Enum.CameraType.Custom
end
end,
})

– FOV slider
PlayerVisGroup:CreateSlider({
Name     = “Field of View”,
Flag     = “FOV”,
Min      = 30,
Max      = 120,
Default  = 70,
Rounding = 1,
Callback = function(v)
workspace.CurrentCamera.FieldOfView = v
end,
})

– Utilities
PlayerUtilGroup:CreateButton({
Name     = “Copy Username”,
Flag     = “Copy Username”,
Callback = function()
pcall(function() if setclipboard then setclipboard(Player.Name) end end)
notify(“Player”, “Username copied: “ .. Player.Name, 3)
end,
})

PlayerUtilGroup:CreateButton({
Name     = “Copy User ID”,
Flag     = “Copy User ID”,
Callback = function()
pcall(function() if setclipboard then setclipboard(tostring(Player.UserId)) end end)
notify(“Player”, “UserID copied: “ .. Player.UserId, 3)
end,
})

PlayerUtilGroup:CreateButton({
Name     = “Rejoin Server”,
Flag     = “Rejoin Server”,
Callback = function()
notify(“Player”, “Rejoining…”, 3)
task.wait(1)
local TS = game:GetService(“TeleportService”)
TS:Teleport(game.PlaceId, Player)
end,
})

PlayerUtilGroup:CreateButton({
Name     = “Leave Server”,
Flag     = “Leave Server”,
Callback = function()
notify(“Player”, “Leaving server…”, 3)
task.wait(1)
game:GetService(“TeleportService”):Teleport(game.PlaceId)
end,
})

PlayerUtilGroup:CreateToggle({
Name     = “Anti AFK”,
Flag     = “Anti AFK”,
Default  = false,
Callback = function(v)
SetToggleState(“Anti AFK”, v)
if v then
local VirtualUser = game:GetService(“VirtualUser”)
Player.Idled:Connect(function()
if GetToggleState(“Anti AFK”) then
VirtualUser:CaptureController()
VirtualUser:ClickButton2(Vector2.new())
end
end)
end
end,
})

PlayerUtilGroup:CreateToggle({
Name     = “Show Player Pings”,
Flag     = “Show Pings”,
Default  = false,
Callback = function(v)
SetToggleState(“Show Pings”, v)
if not v then return end
task.spawn(function()
while GetToggleState(“Show Pings”) do
local stats = game:GetService(“Stats”)
local ping  = stats.Network.ServerStatsItem[“Data Ping”]:GetValue()
notify(“Ping”, “Your ping: “ .. math.floor(ping) .. “ms”, 4)
task.wait(5)
end
end)
end,
})

– ============================================================
–  MISC TAB
– ============================================================
local MiscTrollGroup  = Tabs.Misc:CreateBlock({Name = “Trolling”,         Side = “Left”})
local MiscChatGroup   = Tabs.Misc:CreateBlock({Name = “Chat Tools”,       Side = “Left”})
local MiscWorldGroup  = Tabs.Misc:CreateBlock({Name = “World”,            Side = “Left”})
local MiscToolsGroup  = Tabs.Misc:CreateBlock({Name = “Server Tools”,     Side = “Right”})
local MiscSpamGroup   = Tabs.Misc:CreateBlock({Name = “Spam”,             Side = “Right”})

– Trolling
local crashServerActive = false
MiscTrollGroup:CreateToggle({
Name     = “Lag Server (object spam)”,
Flag     = “Lag Server”,
Default  = false,
Callback = function(v)
SetToggleState(“Lag Server”, v)
crashServerActive = v
if not v then return end
task.spawn(function()
local SpawnRemote = RS:FindFirstChild(“MenuToys”) and RS.MenuToys:FindFirstChild(“SpawnToyRemoteFunction”)
if not SpawnRemote then notify(“Misc”, “SpawnRemote not found!”, 3); return end
while crashServerActive do
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if hrp then
pcall(function()
SpawnRemote:InvokeServer(“BallSnowball”, hrp.CFrame * CFrame.new(math.random(-5,5), 5, math.random(-5,5)), Vector3.zero)
end)
end
task.wait(0.05)
end
end)
end,
})

MiscTrollGroup:CreateToggle({
Name     = “Disco Lighting”,
Flag     = “Disco Lighting”,
Default  = false,
Callback = function(v)
SetToggleState(“Disco Lighting”, v)
if not v then return end
task.spawn(function()
local colors = {
Color3.fromRGB(255, 0, 0),
Color3.fromRGB(0, 255, 0),
Color3.fromRGB(0, 0, 255),
Color3.fromRGB(255, 255, 0),
Color3.fromRGB(255, 0, 255),
Color3.fromRGB(0, 255, 255),
}
local i = 1
local lighting = game:GetService(“Lighting”)
while GetToggleState(“Disco Lighting”) do
lighting.Ambient = colors[i]
lighting.OutdoorAmbient = colors[i]
i = (i % #colors) + 1
task.wait(0.12)
end
end)
end,
})

MiscTrollGroup:CreateToggle({
Name     = “Earthquake (camera shake)”,
Flag     = “Earthquake”,
Default  = false,
Callback = function(v)
SetToggleState(“Earthquake”, v)
if not v then return end
task.spawn(function()
local cam = workspace.CurrentCamera
while GetToggleState(“Earthquake”) do
local shake = CFrame.Angles(
math.rad(math.random(-3, 3)),
math.rad(math.random(-3, 3)),
math.rad(math.random(-3, 3))
)
cam.CFrame = cam.CFrame * shake
task.wait(0.03)
end
end)
end,
})

– Night mode
MiscWorldGroup:CreateToggle({
Name     = “Night Mode”,
Flag     = “Night Mode”,
Default  = false,
Callback = function(v)
SetToggleState(“Night Mode”, v)
local lighting = game:GetService(“Lighting”)
lighting.ClockTime = v and 0 or 14
lighting.Brightness = v and 0 or 1
end,
})

– Time slider
MiscWorldGroup:CreateSlider({
Name     = “Time of Day”,
Flag     = “Time of Day”,
Min      = 0,
Max      = 24,
Default  = 14,
Rounding = 1,
Callback = function(v)
game:GetService(“Lighting”).ClockTime = v
end,
})

– Remove fog
MiscWorldGroup:CreateToggle({
Name     = “Remove Fog”,
Flag     = “Remove Fog”,
Default  = false,
Callback = function(v)
SetToggleState(“Remove Fog”, v)
local lighting = game:GetService(“Lighting”)
lighting.FogEnd   = v and 100000 or 1000
lighting.FogStart = v and 100000 or 0
end,
})

– Delete all parts named PaintPlayerPart in workspace
MiscWorldGroup:CreateButton({
Name     = “Clear All Paint Parts”,
Flag     = “Clear Paint Parts”,
Callback = function()
local count = 0
for _, obj in pairs(workspace:GetDescendants()) do
if obj.Name == “PaintPlayerPart” then obj:Destroy(); count = count + 1 end
end
notify(“Misc”, “Cleared “ .. count .. “ paint parts.”, 3)
end,
})

– Delete all toys in your folder
MiscWorldGroup:CreateButton({
Name     = “Clear My Toys”,
Flag     = “Clear My Toys”,
Callback = function()
local folder = workspace:FindFirstChild(Player.Name .. “SpawnedInToys”)
if not folder then notify(“Misc”, “No toys folder found!”, 3); return end
local destroyRem = RS:FindFirstChild(“MenuToys”) and RS.MenuToys:FindFirstChild(“DestroyToy”)
local count = 0
for _, toy in pairs(folder:GetChildren()) do
pcall(function()
if destroyRem then destroyRem:FireServer(toy)
else toy:Destroy() end
end)
count = count + 1
end
notify(“Misc”, “Cleared “ .. count .. “ toys!”, 3)
end,
})

– Chat tools
local chatSpamActive = false
local chatSpamMsg    = “hi”
MiscChatGroup:CreateInput({
Name        = “Chat Message”,
Flag        = “Chat Message Input”,
Default     = “hi”,
Placeholder = “Enter message…”,
Numeric     = false,
Finished    = true,
Callback    = function(v) chatSpamMsg = v end,
})

MiscChatGroup:CreateButton({
Name     = “Send Chat Message”,
Flag     = “Send Chat”,
Callback = function()
pcall(function()
local chatEvents = RS:FindFirstChild(“DefaultChatSystemChatEvents”)
if chatEvents then
local say = chatEvents:FindFirstChild(“SayMessageRequest”)
if say then say:FireServer(chatSpamMsg, “All”) end
end
end)
end,
})

MiscChatGroup:CreateToggle({
Name     = “Chat Spam”,
Flag     = “Chat Spam”,
Default  = false,
Callback = function(v)
SetToggleState(“Chat Spam”, v)
chatSpamActive = v
if not v then return end
task.spawn(function()
while chatSpamActive do
pcall(function()
local chatEvents = RS:FindFirstChild(“DefaultChatSystemChatEvents”)
if chatEvents then
local say = chatEvents:FindFirstChild(“SayMessageRequest”)
if say then say:FireServer(chatSpamMsg, “All”) end
end
end)
task.wait(0.5)
end
end)
end,
})

MiscChatGroup:CreateButton({
Name     = “Print All Players to Chat”,
Flag     = “Print Players”,
Callback = function()
local names = {}
for _, plr in pairs(PS:GetPlayers()) do
table.insert(names, plr.Name)
end
local msg = “Players: “ .. table.concat(names, “, “)
pcall(function()
local chatEvents = RS:FindFirstChild(“DefaultChatSystemChatEvents”)
if chatEvents then
local say = chatEvents:FindFirstChild(“SayMessageRequest”)
if say then say:FireServer(msg, “All”) end
end
end)
end,
})

– Server Tools
MiscToolsGroup:CreateButton({
Name     = “Print All Players (console)”,
Flag     = “Print All Players”,
Callback = function()
for _, plr in pairs(PS:GetPlayers()) do
print(plr.Name .. “ | ID: “ .. plr.UserId .. “ | Ping: N/A”)
end
notify(“Misc”, “Printed “ .. #PS:GetPlayers() .. “ players to console.”, 3)
end,
})

MiscToolsGroup:CreateButton({
Name     = “Copy Server Job ID”,
Flag     = “Copy Job ID”,
Callback = function()
pcall(function() if setclipboard then setclipboard(game.JobId) end end)
notify(“Misc”, “Job ID copied!”, 3)
end,
})

MiscToolsGroup:CreateButton({
Name     = “Copy Game Place ID”,
Flag     = “Copy Place ID”,
Callback = function()
pcall(function() if setclipboard then setclipboard(tostring(game.PlaceId)) end end)
notify(“Misc”, “Place ID copied: “ .. game.PlaceId, 3)
end,
})

MiscToolsGroup:CreateToggle({
Name     = “Show FPS Counter”,
Flag     = “FPS Counter”,
Default  = false,
Callback = function(v)
SetToggleState(“FPS Counter”, v)
if not v then return end
task.spawn(function()
while GetToggleState(“FPS Counter”) do
local fps = math.floor(1 / R.RenderStepped:Wait())
notify(“FPS”, “Current FPS: “ .. fps, 3)
task.wait(3)
end
end)
end,
})

– Spam
local spawnSpamActive = false
MiscSpamGroup:CreateToggle({
Name     = “Toy Spawn Spam (snowballs)”,
Flag     = “Toy Spawn Spam”,
Default  = false,
Callback = function(v)
SetToggleState(“Toy Spawn Spam”, v)
spawnSpamActive = v
if not v then return end
task.spawn(function()
local SpawnRemote = RS:FindFirstChild(“MenuToys”) and RS.MenuToys:FindFirstChild(“SpawnToyRemoteFunction”)
if not SpawnRemote then return end
while spawnSpamActive do
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if hrp then
pcall(function()
SpawnRemote:InvokeServer(“BallSnowball”, hrp.CFrame * CFrame.new(0, 5, -5), Vector3.new(0, 0, -100))
end)
end
task.wait(0.1)
end
end)
end,
})

MiscSpamGroup:CreateToggle({
Name     = “Banana Spam (on target)”,
Flag     = “Banana Spam”,
Default  = false,
Callback = function(v)
SetToggleState(“Banana Spam”, v)
if not v then return end
task.spawn(function()
local SpawnRemote = RS:FindFirstChild(“MenuToys”) and RS.MenuToys:FindFirstChild(“SpawnToyRemoteFunction”)
if not SpawnRemote then return end
while GetToggleState(“Banana Spam”) do
local target = selectedKickPlayer
if target and target.Character then
local tRoot = target.Character:FindFirstChild(“HumanoidRootPart”)
if tRoot then
pcall(function()
SpawnRemote:InvokeServer(“BallBanana”, tRoot.CFrame * CFrame.new(0, 3, 0), Vector3.new(0, -50, 0))
end)
end
end
task.wait(0.15)
end
end)
end,
})

– ============================================================
–  BUILD TAB
– ============================================================
local BuildPlaceGroup  = Tabs.Build:CreateBlock({Name = “Placement”,       Side = “Left”})
local BuildToolsGroup  = Tabs.Build:CreateBlock({Name = “Build Tools”,     Side = “Left”})
local BuildSpawnGroup  = Tabs.Build:CreateBlock({Name = “Toy Spawner”,     Side = “Right”})
local BuildDeleteGroup = Tabs.Build:CreateBlock({Name = “Delete & Clear”,  Side = “Right”})

– Placement ghost (highlight placed parts)
BuildPlaceGroup:CreateToggle({
Name     = “Highlight Placed Parts”,
Flag     = “Highlight Placed”,
Default  = false,
Callback = function(v)
SetToggleState(“Highlight Placed”, v)
local folder = workspace:FindFirstChild(Player.Name .. “SpawnedInToys”)
if not folder then return end
local function addHL(obj)
if obj:IsA(“BasePart”) or obj:IsA(“Model”) then
if not obj:FindFirstChild(“BuildHL”) then
local hl = Instance.new(“Highlight”)
hl.Name = “BuildHL”
hl.FillColor = Color3.fromRGB(0, 200, 100)
hl.OutlineColor = Color3.fromRGB(255, 255, 255)
hl.FillTransparency = 0.5
hl.Parent = obj
end
end
end
if v then
for _, obj in pairs(folder:GetChildren()) do addHL(obj) end
folder.ChildAdded:Connect(function(obj)
if GetToggleState(“Highlight Placed”) then addHL(obj) end
end)
else
for _, obj in pairs(folder:GetDescendants()) do
local hl = obj:FindFirstChild(“BuildHL”)
if hl then hl:Destroy() end
end
end
end,
})

– Part count display
BuildPlaceGroup:CreateButton({
Name     = “Show My Part Count”,
Flag     = “Show Part Count”,
Callback = function()
local folder = workspace:FindFirstChild(Player.Name .. “SpawnedInToys”)
local count  = folder and #folder:GetChildren() or 0
notify(“Build”, “You have “ .. count .. “ toys/parts placed.”, 4)
end,
})

– Snap to grid toggle (just a conceptual reminder)
BuildPlaceGroup:CreateToggle({
Name     = “No Collide All My Parts”,
Flag     = “No Collide My Parts”,
Default  = false,
Callback = function(v)
SetToggleState(“No Collide My Parts”, v)
local folder = workspace:FindFirstChild(Player.Name .. “SpawnedInToys”)
if not folder then return end
for _, obj in pairs(folder:GetDescendants()) do
if obj:IsA(“BasePart”) then obj.CanCollide = not v end
end
end,
})

– Build tools
BuildToolsGroup:CreateButton({
Name     = “Anchor All My Parts”,
Flag     = “Anchor All”,
Callback = function()
local folder = workspace:FindFirstChild(Player.Name .. “SpawnedInToys”)
if not folder then notify(“Build”, “No parts folder!”, 3); return end
local count = 0
for _, obj in pairs(folder:GetDescendants()) do
if obj:IsA(“BasePart”) then obj.Anchored = true; count = count + 1 end
end
notify(“Build”, “Anchored “ .. count .. “ parts!”, 3)
end,
})

BuildToolsGroup:CreateButton({
Name     = “Unanchor All My Parts”,
Flag     = “Unanchor All”,
Callback = function()
local folder = workspace:FindFirstChild(Player.Name .. “SpawnedInToys”)
if not folder then notify(“Build”, “No parts folder!”, 3); return end
local count = 0
for _, obj in pairs(folder:GetDescendants()) do
if obj:IsA(“BasePart”) then obj.Anchored = false; count = count + 1 end
end
notify(“Build”, “Unanchored “ .. count .. “ parts!”, 3)
end,
})

BuildToolsGroup:CreateButton({
Name     = “Make All My Parts Invisible”,
Flag     = “Invisible Parts”,
Callback = function()
local folder = workspace:FindFirstChild(Player.Name .. “SpawnedInToys”)
if not folder then notify(“Build”, “No parts folder!”, 3); return end
for _, obj in pairs(folder:GetDescendants()) do
if obj:IsA(“BasePart”) then obj.Transparency = 1 end
end
notify(“Build”, “Made all parts invisible!”, 3)
end,
})

BuildToolsGroup:CreateButton({
Name     = “Make All My Parts Visible”,
Flag     = “Visible Parts”,
Callback = function()
local folder = workspace:FindFirstChild(Player.Name .. “SpawnedInToys”)
if not folder then notify(“Build”, “No parts folder!”, 3); return end
for _, obj in pairs(folder:GetDescendants()) do
if obj:IsA(“BasePart”) then obj.Transparency = 0 end
end
notify(“Build”, “Made all parts visible!”, 3)
end,
})

– Toy spawner
local toySpawnName = “BallSnowball”
BuildSpawnGroup:CreateDropdown({
Name    = “Select Toy to Spawn”,
List    = {
“BallSnowball”, “BallBanana”, “NinjaShuriken”, “CreatureBlobman”,
“TractorGreen”, “PalletLightBrown”, “ToyRocket”, “ToyBomb”,
},
Default = “BallSnowball”,
Callback = function(v) toySpawnName = v end,
})

BuildSpawnGroup:CreateButton({
Name     = “Spawn Selected Toy”,
Flag     = “Spawn Toy”,
Callback = function()
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if not hrp then return end
local SpawnRemote = RS:FindFirstChild(“MenuToys”) and RS.MenuToys:FindFirstChild(“SpawnToyRemoteFunction”)
if not SpawnRemote then notify(“Build”, “SpawnRemote not available!”, 3); return end
pcall(function()
SpawnRemote:InvokeServer(toySpawnName, hrp.CFrame * CFrame.new(0, 5, -5), Vector3.zero)
end)
notify(“Build”, “Spawned: “ .. toySpawnName, 2)
end,
})

BuildSpawnGroup:CreateToggle({
Name     = “Loop Spawn Toy”,
Flag     = “Loop Spawn Toy”,
Default  = false,
Callback = function(v)
SetToggleState(“Loop Spawn Toy”, v)
if not v then return end
task.spawn(function()
local SpawnRemote = RS:FindFirstChild(“MenuToys”) and RS.MenuToys:FindFirstChild(“SpawnToyRemoteFunction”)
if not SpawnRemote then return end
while GetToggleState(“Loop Spawn Toy”) do
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if hrp then
pcall(function()
SpawnRemote:InvokeServer(toySpawnName, hrp.CFrame * CFrame.new(math.random(-5,5), 5, math.random(-5,5)), Vector3.zero)
end)
end
task.wait(0.3)
end
end)
end,
})

– Delete & clear
BuildDeleteGroup:CreateButton({
Name     = “Delete ALL My Toys”,
Flag     = “Delete All Toys”,
Callback = function()
local folder = workspace:FindFirstChild(Player.Name .. “SpawnedInToys”)
if not folder then notify(“Build”, “No toys folder!”, 3); return end
local destroyRem = RS:FindFirstChild(“MenuToys”) and RS.MenuToys:FindFirstChild(“DestroyToy”)
local count = 0
for _, toy in pairs(folder:GetChildren()) do
pcall(function()
if destroyRem then destroyRem:FireServer(toy) else toy:Destroy() end
end)
count = count + 1
end
notify(“Build”, “Deleted “ .. count .. “ toys!”, 3)
end,
})

BuildDeleteGroup:CreateButton({
Name     = “Delete Selected Toy Type”,
Flag     = “Delete Toy Type”,
Callback = function()
local folder = workspace:FindFirstChild(Player.Name .. “SpawnedInToys”)
if not folder then notify(“Build”, “No toys folder!”, 3); return end
local destroyRem = RS:FindFirstChild(“MenuToys”) and RS.MenuToys:FindFirstChild(“DestroyToy”)
local count = 0
for _, toy in pairs(folder:GetChildren()) do
if toy.Name == toySpawnName then
pcall(function()
if destroyRem then destroyRem:FireServer(toy) else toy:Destroy() end
end)
count = count + 1
end
end
notify(“Build”, “Deleted “ .. count .. “ “ .. toySpawnName .. “s!”, 3)
end,
})

BuildDeleteGroup:CreateToggle({
Name     = “Auto Delete Enemy Toys”,
Flag     = “Auto Delete Enemy Toys”,
Default  = false,
Callback = function(v)
SetToggleState(“Auto Delete Enemy Toys”, v)
if not v then return end
task.spawn(function()
local destroyRem = RS:FindFirstChild(“MenuToys”) and RS.MenuToys:FindFirstChild(“DestroyToy”)
while GetToggleState(“Auto Delete Enemy Toys”) do
local target = selectedKickPlayer
if target then
local folder = workspace:FindFirstChild(target.Name .. “SpawnedInToys”)
if folder and destroyRem then
for _, toy in pairs(folder:GetChildren()) do
pcall(function() destroyRem:FireServer(toy) end)
end
end
end
task.wait(1)
end
end)
end,
})

– ============================================================
–  KEYBINDS TAB
– ============================================================
local KeybindMainGroup = Tabs.Keybinds:CreateBlock({Name = “Action Keybinds”,   Side = “Left”})
local KeybindTPGroup   = Tabs.Keybinds:CreateBlock({Name = “Teleport Binds”,    Side = “Left”})
local KeybindMiscGroup = Tabs.Keybinds:CreateBlock({Name = “Misc Binds”,        Side = “Right”})

– Track registered keybinds
local keybindConnections = {}
local function clearKeybind(id)
if keybindConnections[id] then
keybindConnections[id]:Disconnect()
keybindConnections[id] = nil
end
end

– Keybind: Toggle Fly
local flyBindKey = Enum.KeyCode.F
KeybindMainGroup:CreateKeybind({
Name     = “Toggle Fly Keybind”,
Flag     = “Keybind Fly”,
Default  = Enum.KeyCode.F,
Callback = function(key)
flyBindKey = key
clearKeybind(“fly”)
keybindConnections[“fly”] = UserInputService.InputBegan:Connect(function(input, gpe)
if gpe then return end
if input.KeyCode == flyBindKey then
local newVal = not GetToggleState(“Fly”)
SetToggleState(“Fly”, newVal)
– re-trigger fly logic
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
local hum  = char and char:FindFirstChildOfClass(“Humanoid”)
if newVal and hrp and hum then
hum.PlatformStand = true
if not flyBodyVel then
flyBodyVel = Instance.new(“BodyVelocity”)
flyBodyVel.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
flyBodyVel.Velocity = Vector3.zero
flyBodyVel.P = 9e4
flyBodyVel.Parent = hrp
end
if not flyBodyGyro then
flyBodyGyro = Instance.new(“BodyGyro”)
flyBodyGyro.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
flyBodyGyro.D = 100
flyBodyGyro.P = 9e4
flyBodyGyro.Parent = hrp
end
flyActive = true
if not flyConn then
flyConn = R.RenderStepped:Connect(function()
if not flyActive or not hrp or not hrp.Parent then return end
local cam = workspace.CurrentCamera
local moveDir = Vector3.zero
if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + cam.CFrame.LookVector end
if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - cam.CFrame.LookVector end
if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - cam.CFrame.RightVector end
if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + cam.CFrame.RightVector end
if UserInputService:IsKeyDown(Enum.KeyCode.Space) then moveDir = moveDir + Vector3.new(0,1,0) end
if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then moveDir = moveDir - Vector3.new(0,1,0) end
flyBodyVel.Velocity = moveDir.Magnitude > 0 and (moveDir.Unit * 60) or Vector3.zero
flyBodyGyro.CFrame  = cam.CFrame
end)
end
else
flyActive = false
if hum then hum.PlatformStand = false end
if flyConn then flyConn:Disconnect(); flyConn = nil end
if flyBodyVel then flyBodyVel:Destroy(); flyBodyVel = nil end
if flyBodyGyro then flyBodyGyro:Destroy(); flyBodyGyro = nil end
end
notify(“Keybind”, “Fly: “ .. (newVal and “ON” or “OFF”), 2)
end
end)
end,
})

– Keybind: Save position
local savePosKey = Enum.KeyCode.J
KeybindMainGroup:CreateKeybind({
Name     = “Save Position Keybind”,
Flag     = “Keybind Save Pos”,
Default  = Enum.KeyCode.J,
Callback = function(key)
savePosKey = key
clearKeybind(“savepos”)
keybindConnections[“savepos”] = UserInputService.InputBegan:Connect(function(input, gpe)
if gpe then return end
if input.KeyCode == savePosKey then
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if hrp then
savedPlayerPos = hrp.CFrame
notify(“Keybind”, “Position saved!”, 2)
end
end
end)
end,
})

– Keybind: Load position
local loadPosKey = Enum.KeyCode.K
KeybindMainGroup:CreateKeybind({
Name     = “Load Position Keybind”,
Flag     = “Keybind Load Pos”,
Default  = Enum.KeyCode.K,
Callback = function(key)
loadPosKey = key
clearKeybind(“loadpos”)
keybindConnections[“loadpos”] = UserInputService.InputBegan:Connect(function(input, gpe)
if gpe then return end
if input.KeyCode == loadPosKey then
if savedPlayerPos then
local char = Player.Character
local hrp  = char and char:FindFirstChild(“HumanoidRootPart”)
if hrp then hrp.CFrame = savedPlayerPos; notify(“Keybind”, “Position loaded!”, 2) end
else
notify(“Keybind”, “No position saved!”, 2)
end
end
end)
end,
})

– Keybind: Reset character
local resetCharKey = Enum.KeyCode.R
KeybindMainGroup:CreateKeybind({
Name     = “Reset Character Keybind”,
Flag     = “Keybind Reset Char”,
Default  = Enum.KeyCode.R,
Callback = function(key)
resetCharKey = key
clearKeybind(“resetchar”)
keybindConnections[“resetchar”] = UserInputService.InputBegan:Connect(function(input, gpe)
if gpe then return end
if input.KeyCode == resetCharKey then
local char = Player.Character
local hum  = char and char:FindFirstChildOfClass(“Humanoid”)
if hum then hum.Health = 0 end
end
end)
end,
})

– Keybind: TP to selected target
local tpTargetKey = Enum.KeyCode.T
KeybindTPGroup:CreateKeybind({
Name     = “TP To Target Keybind”,
Flag     = “Keybind TP Target”,
Default  = Enum.KeyCode.T,
Callback = function(key)
tpTargetKey = key
clearKeybind(“tptarget”)
keybindConnections[“tptarget”] = UserInputService.InputBegan:Connect(function(input, gpe)
if gpe then return end
if input.KeyCode == tpTargetKey then
local target = selectedKickPlayer
if not target or not target.Character then notify(“Keybind”, “No target selected!”, 2); return end
local char  = Player.Character
local hrp   = char and char:FindFirstChild(“HumanoidRootPart”)
local tRoot = target.Character:FindFirstChild(“HumanoidRootPart”)
if hrp and tRoot then
hrp.CFrame = tRoot.CFrame * CFrame.new(0, 0, 3)
notify(“Keybind”, “TP’d to “ .. target.Name, 2)
end
end
end)
end,
})

– Keybind: Bring target to you
local bringTargetKey = Enum.KeyCode.B
KeybindTPGroup:CreateKeybind({
Name     = “Bring Target Keybind”,
Flag     = “Keybind Bring Target”,
Default  = Enum.KeyCode.B,
Callback = function(key)
bringTargetKey = key
clearKeybind(“bringtarget”)
keybindConnections[“bringtarget”] = UserInputService.InputBegan:Connect(function(input, gpe)
if gpe then return end
if input.KeyCode == bringTargetKey then
local target = selectedKickPlayer
if not target or not target.Character then notify(“Keybind”, “No target!”, 2); return end
local myChar = Player.Character
local myHRP  = myChar and myChar:FindFirstChild(“HumanoidRootPart”)
local tRoot  = target.Character:FindFirstChild(“HumanoidRootPart”)
if myHRP and tRoot then
pcall(function()
RS.GrabEvents.SetNetworkOwner:FireServer(tRoot, tRoot.CFrame)
tRoot.CFrame = myHRP.CFrame * CFrame.new(2, 0, 2)
end)
notify(“Keybind”, “Brought “ .. target.Name .. “ to you!”, 2)
end
end
end)
end,
})

– Keybind: Toggle No Clip
local noClipKey = Enum.KeyCode.N
KeybindMiscGroup:CreateKeybind({
Name     = “No Clip Keybind”,
Flag     = “Keybind No Clip”,
Default  = Enum.KeyCode.N,
Callback = function(key)
noClipKey = key
clearKeybind(“noclip”)
keybindConnections[“noclip”] = UserInputService.InputBegan:Connect(function(input, gpe)
if gpe then return end
if input.KeyCode == noClipKey then
local newVal = not GetToggleState(“No Clip”)
SetToggleState(“No Clip”, newVal)
if noClipConn then noClipConn:Disconnect(); noClipConn = nil end
if newVal then
noClipConn = R.Stepped:Connect(function()
local char = Player.Character
if not char then return end
for _, p in pairs(char:GetDescendants()) do
if p:IsA(“BasePart”) then p.CanCollide = false end
end
end)
else
local char = Player.Character
if char then
for _, p in pairs(char:GetDescendants()) do
if p:IsA(“BasePart”) then p.CanCollide = true end
end
end
end
notify(“Keybind”, “No Clip: “ .. (newVal and “ON” or “OFF”), 2)
end
end)
end,
})

– Keybind: Toggle Infinite Jump
local infJumpKey = Enum.KeyCode.G
KeybindMiscGroup:CreateKeybind({
Name     = “Infinite Jump Keybind”,
Flag     = “Keybind Inf Jump”,
Default  = Enum.KeyCode.G,
Callback = function(key)
infJumpKey = key
clearKeybind(“infjump”)
keybindConnections[“infjump”] = UserInputService.InputBegan:Connect(function(input, gpe)
if gpe then return end
if input.KeyCode == infJumpKey then
local newVal = not GetToggleState(“Infinite Jump”)
SetToggleState(“Infinite Jump”, newVal)
if infJumpConn then infJumpConn:Disconnect(); infJumpConn = nil end
if newVal then
infJumpConn = UserInputService.JumpRequest:Connect(function()
local char = Player.Character
local hum  = char and char:FindFirstChildOfClass(“Humanoid”)
if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
end)
end
notify(“Keybind”, “Inf Jump: “ .. (newVal and “ON” or “OFF”), 2)
end
end)
end,
})

– Keybind: God Mode
local godModeKey = Enum.KeyCode.H
KeybindMiscGroup:CreateKeybind({
Name     = “God Mode Keybind”,
Flag     = “Keybind God Mode”,
Default  = Enum.KeyCode.H,
Callback = function(key)
godModeKey = key
clearKeybind(“godmode”)
keybindConnections[“godmode”] = UserInputService.InputBegan:Connect(function(input, gpe)
if gpe then return end
if input.KeyCode == godModeKey then
local newVal = not GetToggleState(“God Mode”)
SetToggleState(“God Mode”, newVal)
if newVal then
task.spawn(function()
while GetToggleState(“God Mode”) do
local char = Player.Character
local hum  = char and char:FindFirstChildOfClass(“Humanoid”)
if hum then hum.Health = hum.MaxHealth end
task.wait(0.1)
end
end)
end
notify(“Keybind”, “God Mode: “ .. (newVal and “ON” or “OFF”), 2)
end
end)
end,
})

– Keybind: Run method on target
local runMethodKey = Enum.KeyCode.M
KeybindMiscGroup:CreateKeybind({
Name     = “Apply Method Keybind”,
Flag     = “Keybind Apply Method”,
Default  = Enum.KeyCode.M,
Callback = function(key)
runMethodKey = key
clearKeybind(“applymethod”)
keybindConnections[“applymethod”] = UserInputService.InputBegan:Connect(function(input, gpe)
if gpe then return end
if input.KeyCode == runMethodKey then
runMethod(selectedKickPlayer)
notify(“Keybind”, “Method applied!”, 2)
end
end)
end,
})

– ============================================================
–  ALL DONE — FINAL NOTIFY
– ============================================================
task.wait(2)
pcall(function()
notify(“SILENCED HUB”, “✅ All systems loaded! discord.gg/bdy4ssD3P4 | RightShift to toggle UI.”, 6)
end)
